// ─── lib/featureFlags.ts — 출시 범위 플래그 ───────────────────────────────────
//
// 코드는 완성됐지만 **지금 켜면 안 되는** 기능을 한곳에서 끈다. 삭제가 아니라 플래그인
// 이유: 되돌릴 때 구현을 다시 쓰는 게 아니라 값 하나만 바꾸면 되게 하려는 것이다.
//
// 규칙: 여기 있는 플래그는 전부 **끄는 쪽이 안전한 기본값**이어야 한다. 켜는 조건을
// 주석에 명시하고, 그 조건이 충족되기 전엔 켜지 않는다.

/**
 * 월간 랭킹(리더보드) 엔트리를 Firestore `leaderboards/{ym}/entries/{uid}` 에 발행할지.
 *
 * **false 인 이유 (2026-07-29 감사):**
 *  1) 리더보드를 볼 화면 진입점이 없다. `App.tsx` 는 ProgressionScreen 에
 *     `onOpenHallOfFame` 을 주입하지 않고, `setShowHallOfFame(true)` 를 부르는 코드도
 *     없다 — 즉 사용자는 이 기능의 존재조차 모른다.
 *  2) 그런데 발행은 클라우드 동기마다 돌아, **닉네임·월간 거리·일관성 점수**가 다른
 *     로그인 사용자 전원이 읽을 수 있는 컬렉션에 쌓였다.
 *  3) 동의를 받은 적이 없고, 개인정보 처리방침에도 '다른 이용자에게 공개된다'는
 *     고지가 없다.
 *
 * **켜는 조건 셋** — 하나라도 빠지면 같은 사고가 반복된다. 진행 상황(2026-08-01):
 *   ① 공개 범위 옵트인 ............ ✅ 완료(SocialConsentScreen · 설정 토글).
 *      동의 전에는 발행 자체가 막힌다(App.publishMyRankingNow 의 socialVisibility 가드).
 *   ② 랭킹 화면 진입점 ............ ✅ 완료(마이 → 명예의 전당).
 *   ③ 처리방침 제3자 공개 조항 .... ⛔ **미완** — 문안은 docs/legal/social-disclosure.md
 *      에 준비돼 있고, keego-legal 저장소에 반영해 배포해야 한다(민우님 작업).
 *
 * ③ 이 끝나면 이 값을 true 로 바꾼다. **그 전에는 켜지 않는다** — 화면과 동의를 갖춰도
 * 처리방침에 고지가 없으면 "동의 없이 공개"와 법적으로 같은 자리다.
 *
 * 발행 코드(`lib/progression/firestoreRankingStore.publishMyRanking`)와 화면
 * (`HallOfFameScreen.rn.tsx`)은 그대로 살아 있다 — 이 값만 바꾸면 켜진다.
 */
export const LEADERBOARD_PUBLISH_ENABLED = false;

/**
 * 원격 푸시(FCM) 사용 여부 — 서버가 사용자 폰으로 밀어 보내는 알림.
 *
 * **false = 지금은 쓰지 않는다(2026-07-30 민우님 결정).**
 *
 * 왜 안 쓰는가: 보낼 내용이 없다. 앱의 알림 두 종류는 원격 푸시가 필요 없다 —
 *   · 러닝 리마인더 → 폰이 스스로 예약하는 **로컬 알림**(lib/localReminder). 서버 불필요.
 *   · 교체 임박·주간 목표 → 앱을 열 때 계산해 보여준다. 오히려 이게 정확하다(미리 예약해
 *     두면 그 사이 상황이 바뀌어 틀린 알림이 된다).
 * 원격 푸시가 필요해지는 건 **서버만 아는 일**을 알릴 때다: 소셜(친구·챌린지), 운영 공지,
 * 서버가 계산하는 리캡. 셋 다 아직 없다.
 *
 * 끄면 무엇이 달라지나: **FCM 토큰을 아예 받지 않는다.** 안 쓸 토큰을 받아 저장하면
 * 기기 식별자를 목적 없이 보관하는 것이라 개인정보 최소수집 원칙에 어긋난다(처리방침에서
 * '푸시 토큰' 항목을 뺀 이유). 알림 **권한**은 계속 요청한다 — 로컬 알림에 필요하다.
 *
 * 켜는 조건: 위 세 용도 중 하나가 실제로 생기고, 토큰을 받을 서버 엔드포인트가 준비될 때
 * (FCM_REGISTER_ENDPOINT). 그때 이 값을 true 로 바꾼다 — 배선은 그대로 남겨뒀다.
 */
export const REMOTE_PUSH_ENABLED = false;

/**
 * 공개 프로필(`profiles/{uid}`)을 발행할지 — 「이 사람은 무슨 러닝화 신나」.
 *
 * **false 인 이유 (2026-08-02 App Store 심사 감사 B-1):**
 *  1) 발행되는 내용은 닉네임·현역 신발 6켤레·은퇴 신발 12켤레·총거리·이번달 거리·러닝 횟수·
 *     VO₂max·평균 페이스·최장 거리·거리 PB 4종이고, 규칙상 **로그인한 전원이 읽을 수 있다**
 *     (`firestore.rules` `match /profiles/{uid}` → `allow read: if signedIn()`).
 *  2) 그런데 **공개된 처리방침에 '다른 이용자에게 공개된다'는 고지가 없다.** 오히려
 *     `docs/privacy.html` 은 제3자 제공·공개를 하지 않는다고 적어 뒀다.
 *  3) 스토어 개인정보 신고서(`docs/store-privacy-labels.md`)에도 이 항목이 없다 —
 *     실제 동작과 어긋난 신고로 제출된다.
 *  4) **그 프로필을 볼 화면이 앱에 없다.** 포트 계약에 읽기 API 자체가 없다
 *     (`lib/cloudPort.ts` 는 put/delete 뿐). 동의만 받고 아무도 못 본다.
 *
 * 이건 AUDIT 1 이 잡아낸 사고(`767032e` — 동의도 화면도 없이 개인정보가 공개 컬렉션에
 * 쌓이던 것)와 **패턴이 같다.** 컬렉션 이름만 `leaderboards` → `profiles` 로 바뀌었다.
 * LEADERBOARD_PUBLISH_ENABLED 가 지키던 규율을 여기에도 똑같이 적용한다.
 *
 * **끄면 무엇이 달라지나:**
 *  · 발행이 멈춘다. 그리고 **이미 올라가 있던 문서는 내려간다** — `App.tsx` 가 플래그가
 *    꺼져 있으면 프로필을 `null` 로 만들어 `publishProfile` 에 넘기고, 그 함수는 null 을
 *    "지우라"는 뜻으로 읽는다. "안 쓰는 것"이 아니라 "내리는 것"이어야 실제로 안 보인다.
 *  · 공개 범위 동의 화면을 띄우지 않는다 — 꺼진 기능의 동의를 받는 건 무의미하다.
 *
 * **켜는 조건 셋** — 리더보드와 같다. 하나라도 빠지면 같은 사고가 반복된다.
 *   ① 공개 범위 옵트인 ............ ✅ 완료(SocialConsentScreen · 설정 토글)
 *   ② 프로필을 **볼 화면** ........ ⛔ 미완 — 지금은 읽는 코드가 아예 없다
 *   ③ 처리방침 제3자 공개 조항 .... ⛔ 미완 — 문안은 `docs/legal/social-disclosure.md` 에
 *      준비돼 있고, 공개 저장소 `solelife9/keego-legal` 에 반영해 배포해야 한다(민우님 작업).
 *      이 저장소의 `docs/privacy.html` 만 고치면 공개 URL 은 옛 내용 그대로다.
 *
 * ②③ 이 끝나면 이 값을 true 로 바꾼다. 발행 코드(`lib/publicProfile`)와 동의 화면
 * (`SocialConsentScreen.rn.tsx`)은 그대로 살아 있다 — 이 값만 바꾸면 켜진다.
 */
export const SOCIAL_PROFILE_PUBLISH_ENABLED = false;
