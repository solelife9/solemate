<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- audit#1 백그라운드 트래킹: 화면off/앱 백그라운드에서도 거리·시간 기록을
         이어가기 위한 location 타입 포그라운드 서비스 권한(사용자 승인된 예외).
         ACCESS_BACKGROUND_LOCATION은 Android 10+에서 백그라운드 위치 접근에 필요
         (포그라운드 서비스에는 선택이나, Doze 중에도 안정적으로 동작하도록 선언). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!-- audit#1: location 타입 포그라운드 서비스 선언 위치. 포그라운드 서비스를
           제공하는 geolocation 모듈은 자신의 라이브러리 매니페스트에서 아래 형태의
           <service>를 머지한다:
             <service android:name="<제공자 서비스 클래스>"
                      android:foregroundServiceType="location"
                      android:exported="false" />
           현재 설치된 react-native-geolocation-service@5.3.1은 포그라운드 서비스
           클래스를 제공하지 않으므로(실재 클래스 없음) 여기서 임의 클래스명을
           선언하면 런타임 크래시 위험이 있어 선언하지 않는다. 위 FOREGROUND_SERVICE*
           권한은 미리 선언해 두어, 서비스 제공 모듈로 교체/업그레이드 시 매니페스트
           머지만으로 즉시 활성화된다(.tenet/knowledge 참고). -->
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
