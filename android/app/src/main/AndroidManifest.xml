<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 백그라운드 런 트래킹(화면off) 권한 — expo-location 교체로 실제 동작한다.
         expo-location 의 location 타입 포그라운드 서비스가
         Location.startLocationUpdatesAsync(+foregroundService) 로 기동되므로,
         아래 권한이 실제 기능에 대응한다("화면을 꺼도 거리/시간이 계속 기록됨").
         - FOREGROUND_SERVICE / FOREGROUND_SERVICE_LOCATION: Android 14+ 에서 location
           타입 포그라운드 서비스 기동에 필수.
         - ACCESS_BACKGROUND_LOCATION: Android 10(API 29)+ 에서 화면off/백그라운드
           위치 수집에 필요(런타임 요청은 requestBackgroundPermissionsAsync 가 담당하며,
           거부돼도 포그라운드 트래킹은 graceful 하게 동작).
         location 타입 <service> 선언은 expo-location 모듈의 매니페스트가 머지하므로
         여기서 중복 선언하지 않는다(중복 선언 시 머지 충돌/크래시 위험). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />


    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!-- location 타입 포그라운드 서비스(<service ... foregroundServiceType="location">)
           는 expo-location 모듈의 AndroidManifest 가 자동 머지하므로 여기서 직접
           선언하지 않는다(중복 선언 시 머지 충돌). -->
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
