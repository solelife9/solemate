# 민우님 할 일 — 출시 전 체크리스트

> 갱신 2026-08-01 · 기준 커밋 `ca26705`
> **여기만 보면 됩니다.** 제가 못 하는 것(콘솔 클릭·서류·실기기·결정)만 모았습니다.
> 코드로 할 수 있는 건 전부 끝나 있습니다. 하나씩 지우시면 됩니다.

---

## 🔥 오늘/이번 주 — 늦으면 출시일이 밀립니다

- [ ] **① 위치기반서비스사업 신고 (방통위)** — ⏳ **수 주 소요**

  **중요해서가 아니라 남이 처리하는 시간이 제일 길어서 1번입니다.** 접수해두고 나머지를
  진행하면 됩니다. 이것만 늦어지면 다른 게 다 끝나도 출시를 못 합니다.
  - 제출용 설명자료는 만들어 뒀습니다 → `docs/legal/location-data-flow.md`
  - 위치정보 수집·저장·삭제 구조가 다 적혀 있어 그대로 첨부하시면 됩니다

- [ ] **② 앱 ID 결정** — ⚠️ **출시하면 영구 고정. 5분이면 됩니다**

  지금 `com.solemate` 인데 브랜드는 **Keego** 입니다.
  나중에 바꾸려면 **완전히 새 앱으로 올려야 하고**, 기존 사용자·리뷰·순위·다운로드가
  전부 끊깁니다. 바꿀 거면 지금이 마지막 기회입니다.
  - 현재: `android/app/build.gradle` · `ios/SoleMate.xcodeproj` 양쪽 다 `com.solemate`
  - **바꾸기로 하시면 말씀해 주세요.** Firebase·카카오·네이버 등록 정보까지 같이 손봐야 합니다

---

## 💰 콘솔 작업 — 합쳐서 30분, 과금 사고 방어선

> 읽기/쓰기를 43배 줄여놨지만 **그건 정상 사용자 기준**입니다.
> 남용이 끼면 그 계산은 통째로 무의미해집니다. 아래가 그 방어선입니다.

- [ ] **③ Firebase 예산 알림** — 이상 과금의 **유일한 조기 경보**

  Blaze 요금제는 상한이 없습니다. 지금은 이상하게 돈이 새도 알 방법이 없습니다.
  - GCP 콘솔 → 결제 → **예산 및 알림** → 예산 만들기
  - 금액은 월 $10~20 정도로 잡고 50%/90%/100% 알림이면 충분합니다
    (정상이면 사용자 1만 명에도 월 $4 수준입니다)

- [ ] **④ Google Maps 키 제한** — 지금은 **아무나 이 키로 지도를 쓰고 민우님께 청구**됩니다

  - Cloud Console → API 및 서비스 → 사용자 인증 정보 → 해당 키
  - **애플리케이션 제한**: Android 앱 → 패키지명 `com.solemate` + SHA-1 `39:D4:…:67:CE`
  - **API 제한**: Maps SDK for Android 만 허용

- [ ] **⑤ App Check enforce 켜기** — 지금은 붙어만 있고 **실제 차단은 꺼져 있습니다**

  - Firebase 콘솔 → App Check → iOS 앱에 **App Attest** 등록
  - 지표에서 미검증 요청이 0에 수렴한 뒤 **Firestore/Auth 적용(enforce)** 토글
  - ⚠️ 순서 지켜주세요. 먼저 켜면 아직 업데이트 안 한 사용자가 잠깁니다

- [ ] **⑥ 서비스 계정 키 정리** — 어제 받으신 키 **2개**가 아직 살아 있습니다

  프로젝트 전체 관리자 권한이라, 규칙을 우회해 **모든 사용자 데이터를 읽고 지울 수**
  있는 파일입니다.
  ```
  rm ~/Downloads/keego-620b8-firebase-adminsdk-*.json
  ```
  파일만 지우는 것과 키를 폐기하는 건 다릅니다 — **콘솔에서도 폐기**해 주세요:
  Firebase 콘솔 → 프로젝트 설정 → 서비스 계정 → 키 관리 → 안 쓰는 키 삭제

---

## 📱 실기기·빌드 — 출시 전 반드시

- [ ] **⑦ 안드로이드 실기기 러닝** — ⚠️ **거리 정확도를 한 번도 안 재봤습니다**

  러닝 앱의 핵심 기능인데 갤럭시에서 미검증 상태입니다. 아이폰 튜닝값도 실측 재확인이
  필요합니다. 30분 이상, 야외에서, 가능하면 가민/애플워치와 동시에.

- [ ] **⑧ 위젯 홈 화면 배치 확인** — 에뮬레이터로는 못 얹었습니다(목록 미리보기까지는 정상)

- [ ] **⑨ `aps-environment` → production 확인** (Xcode 실빌드) — iOS 푸시

- [ ] **⑩ Play 재서명 지문 등록** — 첫 업로드 **후** Play Console에서 확인 → Firebase·카카오에 추가

  ⚠️ 빠뜨리면 **스토어에서 받은 앱만 로그인이 전멸**합니다. 내부 테스트로는 재현이 안 됩니다.

- [ ] **⑪ 스토어 링크 등록** — 앱스토어 ID 확정 후

  ```
  node scripts/seed-config.mjs --ios <URL> --android <URL>
  ```
  없어도 동작은 합니다("스토어에서 Keego 검색" 안내로 폴백). 있으면 버튼 한 번에 갑니다.

---

## 🟡 결정만 해주시면 제가 합니다

- [ ] **⑫ 탈퇴해도 검색 기록이 남습니다**

  `search_misses`·`shoe_requests` 에 계정 식별자가 함께 저장되는데, 규칙상 앱이
  **찾을 수도 지울 수도 없습니다.** 처리방침("탈퇴 시까지")과 어긋납니다.
  → **권장: 계정 식별자를 아예 저장하지 않기.** 신호의 목적(뭐가 없었나)에 uid는 불필요합니다.

- [ ] **⑬ 사진 클라우드 미백업** — 재설치하면 영구 소실됩니다(화면이 고지는 합니다)

  그대로 갈지, 백업을 붙일지.

- [ ] **⑭ 신발 카탈로그 서버 업로드** — 전수조사 끝나면

- [ ] **⑮ 스펙 소스 통일** — 충돌 36건 판정 후 (`docs/audit/spec-source-conflicts.md`)

- [ ] **⑯ 필수 업데이트 화면 디자인 승인**

  새로 만든 화면이라 민우님이 아직 못 보셨습니다. 목업으로 렌더해 드렸습니다
  (스토어 링크 있을 때 / 없을 때 두 상태). 괜찮으면 그대로 갑니다.

- [ ] **⑰ `[공통 꼬리말]` 내용** — 감사 프롬프트에 계속 있었는데 내용을 못 받았습니다

  알려주시면 `01-security.md`·`02-infra.md`·`CHECKLIST.md` 에 소급 적용하겠습니다.

---

## ✅ 제가 끝낸 것 (참고용 — 하실 것 없습니다)

| | 내용 |
|---|---|
| **AUDIT 1 보안** | Firestore 규칙 공격 35건 실증 전부 차단 · 비밀값 유출 0 · 소셜 로그인 서버 검증 |
| **AUDIT 2 인프라** | 하루 읽기 260→6 · 무료 한도 192명→8,300명 · 강제 업데이트 게이트 신설(현재 off) |
| **계정 격리** | 기기를 함께 써도 다음 사람 화면에 앞사람 기록이 안 뜹니다. **데이터는 보관돼 재로그인하면 복원** |
| **대회 시드** | `races` 82개 재업로드 완료(증분 커서 동작 확인) |
| **경로 정확도** | GPX 내보내기 거리 손실 제거(풀코스 −20.7% → −0.22%) |

게이트: `tsc` 0 · `lint` 0 에러 · **258스위트 2,699테스트** · 규칙 74건 — 전부 그린

---

### 남은 기술 부채 (급하지 않음, 기록용)

- **I-6 리더보드 중복 읽기** — 지금 규칙이 막아 실행되지 않는 코드. 1.1 재개봉 때 함께 처리
- **리더보드 1.1 재개봉 조건** — 공개 범위 옵트인 + 처리방침 제3자 공개 조항 + 점수 서버 재계산
  (단순히 규칙만 되돌리면 안 됩니다)
