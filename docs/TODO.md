# 민우님 할 일 — 출시 전 체크리스트

> 갱신 2026-08-05 · HEAD `7228fcd` 기준 · **여기만 보면 됩니다.**
> 제가 못 하는 것(관공서·콘솔 클릭·실기기·결정)만 모았습니다. 코드로 되는 건 끝나 있습니다.
> 판정 근거와 점수는 `docs/audit/FINAL-readiness.md`.
>
> **총량: 약 22시간.** 하루 3시간이면 8일치인데, 실제 출시일은 그 8일이 아니라
> **①번 대기(수 주)** 가 정합니다. ①만 오늘 넣으면 나머지는 그 안에 다 들어갑니다.

---

## ① 오늘 넣어야 하는 것 — 남이 처리하는 시간이 제일 깁니다 (2시간)

- [ ] **위치기반서비스사업 신고 (방통위)** ⏳ **수 주 소요 · 출시일을 좌우합니다**

  중요해서가 아니라 **기다리는 시간이 제일 길어서** 1번입니다. 접수해 두고 나머지를
  진행하면 됩니다. 이것만 늦어지면 다른 게 다 끝나도 출시를 못 합니다.
  - 제출용 기술 설명자료는 만들어 뒀습니다 → `docs/legal/location-data-flow.md`
    (위치정보 수집·저장·삭제 구조가 다 적혀 있어 **그대로 첨부**하시면 됩니다)
  - 남은 건 **사업자 정보 양식 작성**뿐입니다

---

## ② 사용자 0명인 지금이 유일하게 안전한 것 (1.5시간)

> 셋 다 **나중에 하면 사고가 됩니다.** 사용자가 생긴 뒤에 켜면 아직 업데이트 안 한
> 사람이 잠기거나, 이미 새고 난 뒤가 됩니다.

- [ ] **App Check enforce 켜기** — 지금은 붙어만 있고 **실제 차단은 꺼져 있습니다**
  - Firebase 콘솔 → App Check → iOS 앱에 **App Attest** 등록
  - 지표에서 미검증 요청이 0에 수렴한 뒤 **Firestore/Auth 적용(enforce)** 토글
  - ⚠️ 순서 지켜주세요. 먼저 켜면 아직 업데이트 안 한 사용자가 잠깁니다

- [ ] **Google Maps 키 제한** — 지금은 **아무나 이 키로 지도를 쓰고 민우님께 청구**됩니다
  - Cloud Console → API 및 서비스 → 사용자 인증 정보 → 해당 키
  - **애플리케이션 제한**: Android 앱 → 패키지명 **`com.keego.app`** + SHA-1 `39:D4:…:67:CE`
  - **API 제한**: Maps SDK for Android 만 허용
  - ⚠️ **`com.solemate` 아닙니다.** 옛 이름으로 걸면 제한이 걸린 채 지도가 죽습니다
  - ℹ️ 안드로이드를 미뤄도 지금 합니다 — 키는 이미 저장소에 평문으로 있습니다(Android Maps
    키는 바이너리에 박힐 수밖에 없어 '숨기기'가 답이 아닙니다). iOS 는 Apple Maps 를 쓰므로
    이 제한이 iOS 출시를 막지도 깨뜨리지도 않습니다

- [ ] **Firebase 예산 알림** — 이상 과금의 **유일한 조기 경보**
  - GCP 콘솔 → 결제 → **예산 및 알림** → 예산 만들기
  - 월 $10~20, 50%/90%/100% 알림이면 충분합니다(정상이면 1만 명에도 월 $4 수준)

- [ ] **서비스 계정 키 2개 폐기** — 프로젝트 전체 관리자 권한 파일입니다
  ```
  rm ~/Downloads/keego-620b8-firebase-adminsdk-*.json
  ```
  파일만 지우는 것과 키를 폐기하는 건 다릅니다 — **콘솔에서도 폐기**해 주세요:
  Firebase 콘솔 → 프로젝트 설정 → 서비스 계정 → 키 관리 → 안 쓰는 키 삭제

- [ ] **업로드 keystore 백업** — 잃으면 **그 앱은 영원히 업데이트할 수 없습니다**
  - `~/keystores/keego-upload.keystore` + **비밀번호 4개**를 안전한 곳 **두 군데 이상**에
  - 저장소 밖에 있어서(그게 맞습니다) **git 이 지켜주지 않습니다.** 새 키로는 같은 앱을
    갱신 못 하고 새 앱으로 다시 올려야 합니다(리뷰·순위·설치수 전부 초기화)

---

## ③ 빌드 → 실기기 (11시간, 총량의 절반)

- [ ] **빌드 23 아카이브 → TestFlight 업로드 → 폰 설치** (1.5h)
  - 코드는 23 인데 업로드된 건 22 로 알고 있습니다. ASC 에서 확인해 주세요

- [ ] **실기기 QA 8건** (8h) — 러닝 3회에 묶으면 됩니다
  - **1회차(30분 이상, 실외)**: 화면 잠금 → 해제 → 백그라운드(타 앱) → 복귀 →
    비행기모드 on/off → 강제 종료 → 재실행 → **이어 달리기**
    · 볼 것: 거리·경과시간 **연속성**, 케이던스 유지, 복귀 첫 프레임 점프 여부,
      폴리라인 연속성, 공백 구간 미계상
    · ⚠️ **6·7번(잠금·백그라운드)이 이 앱의 코어입니다** — "화면 꺼도 기록된다"가 약속인데
      실측 기록이 없습니다
  - **2회차(짧게)**: 위치 권한 회수 → 재허용(거리 보존 재개) · 비행기모드 완주 저장
  - **3회차(방치 5시간)**: 실제로 달릴 필요 없습니다 — 앱 켠 채 두면(걷기·자전거·가방)
    경로점 1.8만 개 규모와 스냅샷 지연이 재현됩니다. 손 묶이는 시간은 30분 남짓
    · 볼 것: 3·4시간 시점 프레임 드랍·배터리
  - **앉아서**: 시스템 글꼴 **최대 크기**(고정 높이 3곳 글자 잘림) ·
    앱 삭제 후 재설치 첫 로그인(**"첫 탭이 안 먹는다" 미해결 1건**) ·
    신발 하나 지운 뒤 기록 탭에서 그 신발의 런 표시

- [ ] **최근 수정분 눈 확인 4건** (1.5h) — 새로 만든 경로라 봐야 합니다
  - 위치 권한 끈 채 러닝 시작 → 종료: "기록된 러닝이 없어요" + 기록 탭에 아무것도 안 생김
  - 러닝 중 크래시 → 재실행: 로그인 화면이 아니라 **홈에 도달한 뒤** 복구 프롬프트
  - 비행기모드 회원 탈퇴: 15초 뒤 안내 + **계정이 남아 있는가**
  - 비행기모드 완주 저장: 5초 안에 끝나고 위치 라벨만 비는가

- [ ] **위젯 홈 화면 배치 확인** — 에뮬레이터로는 못 얹었습니다(목록 미리보기까지는 정상)

---

## ④ 관측성 — "붙어 있다"와 "도착한다"는 다릅니다 (2시간 + 24시간 대기)

> 배선은 맞는데 콘솔 연결이 끊긴 경우가 흔합니다. **한 번은 실제로 도착하는 걸 봐야 합니다.**

- [ ] **릴리스 빌드에서 의도적으로 크래시 1회** → Crashlytics 도착 + **심볼화(dSYM)** 확인
- [ ] **워치 크래시**도 도착하는지 (워치 Crashlytics 는 신설했습니다)
- [ ] **Analytics 콘솔에 `kg_*` 이벤트 도착** 확인 (첫 수집까지 최대 24시간)

---

## ⑤ 강제 업데이트 게이트 리허설 (1.5시간)

> **비상 장치를 비상 때 처음 써 보면 안 됩니다.** 지금 `config/app` 문서는 **없습니다**(확인함).
> `gcloud auth application-default login` 은 이미 되어 있습니다.

- [ ] 게이트를 **현재 버전과 같게** 켜서 **안 뜨는 것**까지 확인 → 끄기
  ```
  node scripts/seed-config.mjs                 # 현재 상태 보기
  node scripts/seed-config.mjs --min 1.0.0     # 현재 버전과 동일 → 게이트가 뜨면 안 됨
  # 앱 실행해서 필수 업데이트 화면이 안 뜨는 것 확인
  node scripts/seed-config.mjs --off           # 해제
  ```
  ⚠️ 실행하면 "잠금"을 직접 타이핑하는 확인 절차가 한 번 나옵니다(의도된 안전장치)
  ⚠️ **`--min` 은 사용자를 잠그는 스위치입니다.** 실제로 쓸 땐 **고친 버전이 스토어 심사를
     통과한 뒤에만** 넣습니다. 아니면 사용자는 업데이트할 곳 없이 갇힙니다

- [ ] **필수 업데이트 화면 디자인 승인** — 새로 만든 화면이라 아직 못 보셨습니다
      (목업으로 렌더해 드렸습니다 — 스토어 링크 있을 때 / 없을 때 두 상태)

- [ ] **스토어 링크 등록** — 앱스토어 ID 확정 **후에** (없으면 "검색해서 받으세요" 폴백)
  ```
  node scripts/seed-config.mjs --ios <앱스토어 URL>
  ```

---

## ⑥ 스토어 제출 (4시간)

- [ ] **스크린샷 6장 재검증** (2h) — ⚠️ **③ 실기기 QA 뒤에** 하세요(수정이 나오면 다시 찍습니다)
  - 자산은 있습니다: `~/Desktop/keego-스토어-스크린샷/` 6장, 상표 검수까지 마쳤습니다
  - 08-03~04 UX 수정(T4→T3 스윕·ctrlHint·빈 상태·온보딩 헤더)이 그 뒤에 들어갔으니
    **바뀐 화면만** 다시 찍으면 될 가능성이 높습니다. 전면 재제작 아닙니다

- [ ] **ASC 제출 폼** (2h)
  - **Support URL** 입력 (페이지는 게시돼 있습니다 — 200 확인)
  - **App Privacy 답안** — `docs/store-privacy-labels.md` 를 **그대로** 옮기세요
    (기계가 코드·xcprivacy 와 대조해 두는 정본입니다. 임의로 고치면 가드가 빨개집니다)
  - **연령 등급** 설문 (만 14세 미만 대상 아님)
  - **Phased Release 켜기** — 문제가 보일 때 **심사 없이 배포를 멈출 수 있습니다.**
    첫 출시에 가장 값싼 보험이고 토글 하나입니다
  - Expedited Review(신속 심사) 요청 경로만 미리 봐 두기 — 데이터 유실·보안 때만 쓰는 카드

---

## ⑦ 심사 통과 후 — 모집 (1시간 + 대기)

- [ ] **단계 0: 지인 3~5명** 실외 러닝 1회 완주·저장 → **크래시 0** 확인
- [ ] 3일 뒤 **단계 1: 15~20명**(크루 단톡) → 3일 뒤 **단계 2: 50명+**
  - ⚠️ **100명을 한 번에 모으지 마세요.** 첫 출시의 가장 흔한 실패는 모집이 성공해서
    일어납니다 — 100명이 동시에 같은 버그를 겪고 리뷰 100개가 동시에 달립니다
  - 단계 사이 최소 3일 (D1 리텐션은 하루가 지나야 존재하는 숫자입니다)

---

## 🟡 결정만 해주시면 제가 합니다

- [ ] **사진 클라우드 백업** — 지금은 재설치하면 영구 소실됩니다(화면이 고지는 합니다).
      그대로 갈지, 백업을 붙일지
- [ ] **스펙 값 확정 28켤레** — 구조는 고쳤습니다(한 화면에 두 값이 뜨는 일은 없습니다).
      남은 건 어느 값이 맞는지고, 대부분 1~9g·1~2mm 차이라 **출시 후에 하셔도 됩니다.**
      · 딱 하나만 먼저: **Nike Structure 26 = 236g vs 295g (59g 차이)** — 두 등급이라
        하나는 확실히 틀렸습니다(10분)
      · 전체 목록: `docs/audit/04-data-quality.md` §1.3
- [ ] **카테고리 미확정 7켤레** — Adios 9 / Takumi Sen 11 / Streakfly 2 → 레이싱 ·
      Pegasus Premium / Cloudflow 5 → 템포 · Cloudrunner 3 → 안정화 · Cloudsurfer Next → 데일리
- [ ] **이상값 2건** — Mizuno Wave Rebellion Pro 3 힐 **61mm**(대회 상한 40mm 초과) ·
      Topo Trailventure 2 **451g**
- [ ] **신발 카탈로그 서버 업로드** — 전수조사 끝나면
- [ ] **`[공통 꼬리말]` 내용** — 감사 프롬프트에 계속 있었는데 내용을 못 받았습니다

---

## 🔵 안드로이드 — 출시 후 (지금 손대지 않습니다)

> **iOS 단독 선출시가 확정 방침**입니다. 지금 붙이면 일정이 배로 듭니다.
> AAB 는 새 앱 ID 로 다시 구워 뒀으니 옛 산출물이 거짓 신호를 주지도 않습니다.

- [ ] Play Console: 앱 생성 · 데이터 보안 폼 · 콘텐츠 등급
- [ ] **Play App Signing 재서명 SHA-1 을 Firebase·카카오에 추가** — 첫 업로드 **후**에야
      생기는 제3의 키입니다. 빠뜨리면 **스토어에서 받은 앱만 로그인이 전멸**하고
      내부 테스트로는 재현이 안 됩니다
- [ ] 백그라운드 위치 권한 선언 양식 + 시연 영상
- [ ] **안드로이드 실기기 러닝** — 거리 정확도를 갤럭시에서 **한 번도 안 재봤습니다**
- [ ] 스크린샷(폰)·피처 그래픽·짧은/긴 설명

---

## ✅ 하실 것 없습니다 (참고 — 제가 끝냈습니다)

| | 내용 |
|---|---|
| **CI 게이트** | **초록 1회 달성**(run 30959631347) — 284스위트 3,165테스트 · 커버리지 · 규칙 전부 통과. 6번 잘린 원인은 **개발 Node 26 / CI Node 22 불일치**였고 `.nvmrc` 로 단일화했습니다 |
| **CI 가 잡은 결함 2건** | Node 22 에서 테스트가 매달리는 것(미해결로 기록) · `naverAuthClient` 스위트가 **이 노트북의 로컬 키에 의존**해 한 대에서만 통과하던 것 |
| **안드로이드 성능** | 탭 전환 2.1초(탭 화면 재생성)·프레임마다 재그리기(JS 드라이버)·작은 화면 글자 넘침·지도 마커 빈 원 |
| **축하 폭주 버그** | 기존 계정으로 재설치하면 업적 수십 개가 줄줄이 뜨던 것 + '건너뛰기'가 한 개씩만 넘어가던 것 |
| AUDIT 1~5 | 보안(공격 35건 실증) · 인프라(읽기 43배 절감) · 데이터 무결성(유실 13건 판정) · 데이터 품질 · 심사(BLOCKER 3건) · UX(16건) · QA(11건) · 출시 운영 |

**게이트: `tsc` 0 · `lint` 0 에러 · 284스위트 3,165테스트 · 규칙 74건 — 전부 그린**

---

### 남은 기술 부채 (급하지 않음, 기록용)

- **Node 22 에서 테스트가 매달린다** — `engines` 는 `>= 22.11.0` 인데 22 에서 스위트가
  매달립니다. 지금은 개발·CI 모두 26 이라 아무도 안 밟습니다. 남에게 코드를 넘길 때 밟습니다
- **flaky 1건** — `ProfileScreen.haptics.test.tsx` 가 한 번 실패했는데 이후 9회 재현 안 됨.
  원인을 못 밝힌 채 덧대는 건 땜빵이라 기록만 남겼습니다
- I-6 리더보드 중복 읽기(진입 1회 203 읽기) · D-6 오프라인 영속 명시 · D-7 레거시 큐 제거
- 카탈로그 결손(스펙 빈 신발 75 · 연도 427 · 용도 태그 273 · 단종 표시 3) — 빈칸은
  우리 규약상 정직한 상태라 급하지 않습니다
- 랭킹 점수 서버 재계산 — **순위를 상금·보상과 연결하지 않는 한** 피해 없습니다
