# 밤샘 보고서 — 2026-07-11 (금)

> 전권 위임 밤 자율 라운드. 5건 전부 완료 — 각 건 게이트(tsc 클린 · lint 에러 0 · 테스트 1,800) 통과 후 커밋·푸시(`feat/firebase-migration`).

## 한 것 (커밋 체인)

| # | 커밋 | 내용 |
|---|---|---|
| ① | `e53a9c0` | **러닝화 아카이브 무채 통일** — 골드 세리머니 룩(세리프·골드 배경톤·포일 SVG·halo/plinth 무대) 폐지 → 정본 문법(BG+AmbientBackdrop, 콰이어트 글라스, Pretendard TYPE, Rise 진입). 골드는 소액센트 2곳만(최근 헌액 빅넘버 + 헌액 연도 칩). 빈 상태 = 고스트 문법. 인증서 모달도 무채 수렴. 사장된 HALL_* 식장 팔레트 토큰 제거, DESIGN.md §6 예외 조항 삭제 |
| ② | `10039a8` | **리캡 진입 시그니처 모션 v1**(태스크 #10) — 체크 배지 스프링 팝(0.6→1 오버슈트) → '러닝 완료' Rise → 거리 카운트업(0→최종 900ms, tabular) + 스케일 정착 → 이하 섹션 80ms 스태거. reduce-motion 시 전부 즉시 표시 |
| ③ | — | **TestFlight 내부 그룹 배정**(ASC API, 리포 변경 없음 — 아래 상태 참조) |
| ④ | `7ee7875` | **신발탭 fill 에셋 수술 잔재 정리** — 어제 '윗 테두리 갭 메움' 수술이 남긴 세로 대시·점묘 심·반투명 점(총 ~140px)을 알파 마스크 분석으로 제거. 실제 컷아웃(끈 3·밑창 2)만 유지, 실루엣/바운딩은 outline 과 동일 확인 → 탭 전환 형태 점프 없음. 수정 전본 `.bak2` 백업 |
| ⑤ | `05ea615` | **러닝 일시정지 전환 모션** — 링 페이드 퇴장 → 지도 위에서 페이드+슬라이드 인(120ms 뒤) → 거리 히어로·서브 지표 아래로 정착하며 펼침(200ms 뒤). 재개 = 역재생(링 0.9→1 스케일업 복귀). 마운트 타이밍 불변(테스트 보존), reduce-motion 즉시 전환 |

이후: 이 보고서 + 최종 Release 빌드·기기 설치(아래).

### 게이트 결과 (매 커밋마다)
- `npx tsc --noEmit` — 에러 0
- `npm run lint` — 에러 0 (경고 314는 기존 인라인 스타일 등 — 백로그)
  - 참고: HEAD(4fe1cc3) 기준 기존 lint 에러 1건(RunRecapScreen 미사용 GlassEdge import)을 ①에서 함께 해소
- `npm test` — **1,800 / 1,800 통과** (190 스위트)

## 폰에 설치할 것 / 설치 상태

- **Release 빌드 → iPhone 15 Pro(user의 iPhone, 4B701309)** 설치: 아래 '최종 빌드' 참조.
- 잠금 상태라 자동 실행(launch)은 생략 — 아침에 앱 열어서 확인.

## TestFlight 상태 (빌드 2 — 워치 앱 포함)

- 앱: **Keego** (`com.solemate`, App ID 6784201780)
- 내부 그룹 **"keego"** 이미 존재(`isInternalGroup=true`, `hasAccessToAllBuilds=true`)
  → 빌드 2(버전 "2", 상태 **VALID**, 만료 아님)는 **자동으로 내부 그룹에서 접근 가능**. 별도 빌드 배정 불필요.
- 내부 테스터가 0명이어서 팀 멤버(유일 계정) **kji0236@naver.com**(ACCOUNT_HOLDER)을 내부 테스터로 배정 완료 → 현재 상태 **INVITED**.
- 외부 그룹("keego", external)은 건드리지 않음. **외부 공개/심사 제출 안 함**(내부만).

### 아침에 사용자가 할 1클릭
1. **kji0236@naver.com 메일함**에서 TestFlight 초대 메일 열기 → **View in TestFlight / 수락**(또는 TestFlight 앱에 해당 Apple ID 로그인 후 Keego 수락).
2. 아이폰 TestFlight 앱에서 **Keego 빌드 2** 설치.
3. **워치 설치 순서**: 빌드 2가 아이폰에 깔린 뒤 → 아이폰 **Watch 앱 → 사용 가능한 앱 → Keego 설치**(워치 단독앱 페어링 실패 이력 때문에 TestFlight 경유가 정석 경로).

## 아침 확인 체크리스트

- [ ] **러닝화 아카이브 새 룩**(마이 탭): 다른 화면과 같은 무채 글라스인지, 골드는 최근 헌액 빅넘버·연도 칩 2곳뿐인지, 빈 상태(은퇴 0켤레 계정이면) 고스트 문법인지, 인증서 모달 무채+골드 빅넘버.
- [ ] **리캡 진입 모션**(러닝 완료 직후): 배지 팝 → 타이틀 → 거리 카운트업 → 섹션 스태거 리듬이 과하지 않은지(러닝 화면 규칙 = 기기에서 판단). iOS 설정 '동작 줄이기' 시 즉시 표시되는지.
- [ ] **러닝 일시정지/재개 전환**: 일시정지 → 지도가 위에서 자연스럽게 들어오고 지표가 정착하는지, 재개 → 링이 부드럽게 복귀하는지(사용자: "슬라이드처럼").
- [ ] **하단 탭 신발 아이콘**: 신발탭 선택/해제 전환이 홈·시계처럼 자연스러운지(fill 내부 잔재 제거 후).
- [ ] **TestFlight**: 위 1클릭(초대 수락) → 빌드 2 설치 → 워치 앱 설치.

## 최종 빌드 · 설치 결과

- `xcodebuild … -configuration Release` → **BUILD SUCCEEDED** (05ea615 시점, ①②④⑤ 전부 포함)
- `devicectl install` → **iPhone 15 Pro(user의 iPhone, 4B701309)에 설치 완료**
  (`com.solemate`, installationURL 확인). 잠금 상태라 launch 는 생략 — 아침에 직접 실행.
- pbxproj/xcscheme 에 xcodebuild 흔적 없음(작업트리 클린).
