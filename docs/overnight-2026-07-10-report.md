# 🌙 밤샘 작업 보고서 — 2026-07-10 새벽

> 취침 전 마지막 확정: 빈 상태 = 에디토리얼 타이포 + 고스트 1~2장. 이후 자율 작업 원칙 —
> **새 취향 판단 없이, 확정된 정본을 앱 전체에 끝까지 적용 + 위생·출시 준비만.**
> 모든 커밋은 tsc·lint(에러 0)·jest 1,791개 통과 후 푸시. **아침 빌드가 폰에 설치돼 있음.**

## 밤새 한 것 (커밋 6개)

| 커밋 | 내용 |
|---|---|
| `b4393f8` | **카드 서피스 통일 완결** — 마이·진척·리캡·명예의전당·대회기록·신발추가·러닝목표·기록·메달의 잔여 불투명 카드 22곳을 콰이어트 글라스로. 이제 **모든 탭·화면의 카드가 같은 재질**. 바텀시트·드럼피커는 의도적으로 불투명 유지(뒤 비침 방지) |
| `7c8001c` | borderRadius `999` 리터럴 30건 → `RADIUS.pill` 토큰 (시각 동일, 위생) |
| `4181265` | **출시 블로커 해소** — TEMP 온보딩 미리보기를 `__DEV__` 전용으로. **이 빌드부터 실행 시 온보딩이 강제로 뜨지 않는다** (실제 게이트: 신규+신발 0일 때만). dev 빌드에선 미리보기 유지 |
| `fa88189` | **진입 모션 표준** — 홈의 Rise(14px 떠오르는 420ms 페이드)를 primitives로 승격, 신발·기록·마이·진척 탭에 적용. 탭 전환마다 화면이 '켜지는' 리듬이 전 탭 동일 |
| `91a47a5` | **간격 그리드 수렴 1차** — padding/margin/gap의 홀수 임의값 136건을 ±1 스냅(7→8, 13→12, 15→16…). 도트·바 등 크기값은 불변 |
| (이 파일) | 보고서 |

## 아침에 폰에서 확인할 것

1. **탭 4개 전환** — 진입 페이드가 자연스러운지(과하면 duration/거리 토큰 한 줄 조정)
2. **마이탭·진척 화면 카드** — 다른 탭과 같은 유리 표면이 됐는지
3. **앱 재실행** — 온보딩이 더 이상 강제로 안 뜨고 바로 홈으로 가는지
4. 어제 확정분: 보관함/기록/메달 **에디토리얼+고스트 2장** 빈 상태, 메달 카메라 크롭

## 안 한 것 (이유)

- **간격 2차**(rv(1)/rv(3) 미세값, 비4배수 짝수값) — 베이스라인 정렬 의도가 섞여 있어 눈으로 봐야 함
- **스켈레톤/로딩 표준** — 데이터 레이어 판단 필요(어떤 화면이 실제로 '로딩'을 가질지)
- **Card 프리미티브 JSX 전면 교체** — 시각 변화 0인 대형 리팩터라 출시 후로 이연(신규 코드는 Card 의무 — DESIGN.md §5)
- **Firebase Analytics 퍼널** — 네이티브 의존성 사전 승인제라 보류(승인해주면 다음에)

## 남은 출시 트랙 (7월말 목표 크리티컬 패스)

1. 🔴 **GitHub Pages 활성화** (사용자 액션): 리포 Settings → Pages → `main`/`docs` — privacy.html 404 해소
2. 🔴 실기기 QA 풀 시나리오(실외 러닝·백그라운드·크래시 복구·FCM) — `docs/release-checklist.md` §3
3. 🔴 스토어 등록정보·스크린샷·App Privacy (iOS부터 — TestFlight는 이미 됨)
4. 🟠 Android 업로드 키스토어 + Play Console(백그라운드 위치 심사가 최대 리스크 — 8월 각오)

## 태스크 보드 상태

①정본 ②글라스 ③타이포 ④카드 ⑦기기검증 ⑧세로채움 = **완료** · ⑤간격 = 1차 완료(2차는 육안 필요) · ⑥모션 = 진입 모션 완료(스켈레톤 잔여)
