# 실기기 실측 기록

> **적히지 않은 측정은 없는 것과 같다.**
>
> 이 파일은 keego 를 실제 기기로 들고 뛰어서 얻은 숫자를 모으는 곳이다.
> 새 실측을 하면 **여기에 먼저 적고**, 그 값이 코드 상수를 정했다면 해당 주석에도 남긴다.

## 왜 이 파일이 필요했나 (2026-08-07 신설)

`PHANTOM_ACC_FLOOR_FACTOR`(거리 정확도의 핵심 상수) 주석이 *"아직 실측으로 확정되지
않았다 · 안드로이드 실기기 러닝은 한 번도 없다"* 로 남아 있었다. **둘 다 사실이 아니었다** —
2026-08-05 에 네 기기를 동시에 들고 뛴 기록이 있었는데, 그게 **저장소 밖(세션 메모리)에만**
있었다.

그래서 코드를 읽는 사람은 전부 "미검증"으로 판단했고, 감사가 그 항목을 계속 열어 뒀고,
문서끼리 2:1 로 충돌했다.

### 규칙의 빈칸이 원인이다

`CLAUDE.md` 는 이렇게 정한다 — *"구현 변경은 코드+테스트만 바꾼다 · 문서는 규칙·방향이
바뀔 때만 · **git 히스토리 = 작업 기록**"*. 합리적인 규칙인데 **실측이 어느 칸에도 안 들어간다**:

| | 실측은? |
|---|---|
| 구현 변경인가 | 아니다 → 코드도 테스트도 안 바뀐다 → **커밋이 안 생긴다** |
| 규칙·방향 변경인가 | 아니다 → 문서도 안 고친다 |

결과적으로 "git 히스토리 = 작업 기록"이라는 대전제 **밖으로 샜다.** 실측은 채팅으로
도착하고, 듣는 세션이 자기가 나중에 볼 곳(세션 메모리)에 적는다. 그 세션에겐 합리적이지만
**프로젝트에서는 사라진다.**

---

## 기록

### 2026-08-05 · 의왕 5km · 주머니 러닝 (4중 대조) ⭐

가장 중요한 실측. 네 기기를 **동시에** 들고 뛰었다.

| | 가민 | NRC(아이폰) | Keego 아이폰 | Keego 안드로이드 |
|---|---|---|---|---|
| **거리** | 5.07 | 5.03 | 5.03 (−0.8%) | **5.07 (오차 0%)** |
| 고도 | — | 38m | **1,814m ❌** | 32m |
| 케이던스 | 165 | — | — | 0 ❌ |

**이 측정이 확정한 것**
- `PHANTOM_ACC_FLOOR_FACTOR = 0.6` — 07-28 부터 "다음 러닝에서 재비교 필요"로 미확정이던 값.
- **플랫폼별로 계수를 가를 필요가 없다.** 이 계수는 `정확도(m) × factor` 로 도는데 그
  '정확도'가 OS 마다 척도가 다를 수 있어 우려했던 부분이다. 둘 다 기준 안이었다.
- **백그라운드 추적** — 주머니 러닝이었으므로 화면 꺼진 상태 기록이 함께 검증됐다.

**이 측정이 찾아낸 버그 4건** (전부 수정됨)
- 아이폰 고도 1,814m — 모션 권한이 꺼지면 기압계가 조용히 실패하고 GPS 고도로 폴백
- 안드로이드 케이던스 영구 0 — 없는 API 를 부르고 예외를 삼키고 있었다 (`1aeebe4`)
- 실내 러닝이 걸음 권한 없이 0km 로 끝남 (`6920146`)
- 공유 카드가 설계 치수를 벗어나 잘림 (`9988e25`)

**이 측정이 확정하지 *못한* 것 — 다음 실측 과제**
- 그날은 **신호대기가 짧은 코스**였다. 걸음 정지 게이트는 같은 날 "안드로이드에서 여태 한
  번도 동작한 적 없음"이 드러나 되살렸고, 2026-08-07 에 백그라운드 신선도까지 고쳤다.
  **신호등 많은 시내 코스 재측정이 그 게이트의 유일한 판정 조건이다.**
- 고도는 아이폰 값이 오염돼 있어 비교가 성립하지 않았다. **'동작 및 피트니스' 를 켠 상태의
  아이폰 야외 러닝 데이터가 아직 하나도 없다.**

---

### 2026-07-28 · 폰 + 워치 동시

| 가민 | Keego 워치 | Keego 폰 |
|---|---|---|
| 5.43 | 5.358 (−1.3%) | 5.14 (−5.3%) |

- 폰↔워치 **거리 정본을 워치로** 정한 근거(`lib/runMerge.distanceAuthority`).
- 같은 러닝이 두 건으로 저장돼 **신발에 10.50km 가 차감된 사고**가 이날 나왔다 →
  병합(`lib/runMerge`) 도입.
- 이 뒤 `PHANTOM_ACC_FLOOR_FACTOR` 를 0.5 → 0.6 으로 바꿨고, 그 검증이 위 08-05 이다.

---

### 2026-07-20 · 폰 단독

| 가민 | Keego 폰 |
|---|---|
| 3.08 | 3.01 |

- 워치 HK 거리 재설계를 결정한 계기.

---

### 2026-07-13 · 4중 비교

| 가민 | NRC | Keego |
|---|---|---|
| 3.07 | 3.01 | 3.11 (+1.3%) |

- 시간(18:08 vs 18:09) · 케이던스(170 vs 169) · 심박(애플워치 160 / 가민 159) **일치**.
- 거리 +1.3% 는 정상 범위로 보고 보류 — 그 뒤 07-28 에 −5.3% 로 뒤집혔다.
  **한 번의 측정으로 결론 내리면 안 된다는 근거**가 이 줄이다.

### 2026-08-12 · 3중 동시 측정 (가민 · 아이폰 · 갤럭시) — **결함 5건이 드러난 러닝**

같은 러닝을 세 기기로 동시에. 코스: 시내(신호등 구간 포함), 폰은 주머니(화면 꺼짐),
가민은 손목, 애플워치도 손목.

| | 가민 | 아이폰(저장값) | 아이폰(GPS 경로 실측) | 갤럭시 |
|---|---|---|---|---|
| 거리 | **2.96 km** | 3.48 km | **2.95 km** | 2.79 km |
| 시간 | 19:37.5 | 19:35 | — | 19:33 |
| 페이스 | 6'37"/km | 5'37" | **6'38"** | 7'00" |
| 평균 심박 | 150 bpm | 149 | — | `--` |
| 케이던스 | **168 spm** | 160 | — | **1 spm** |
| 상승 고도 | — | 21 m | — | 5 m |

**읽는 법**: 아이폰의 *저장값*과 *GPS 경로 실측*이 다르다. 경로(2.95)는 가민(2.96)과
**10m 차이(0.3%)** 로 정확했고, 저장값(3.48)만 틀렸다 — 워치가 물려준 거리가 얹혔기 때문.
즉 **GPS 정확도는 문제가 없었다.**

여기서 나온 결함(전부 같은 날 수정):

| 증상 | 뿌리 | 수정 |
|---|---|---|
| 아이폰 거리 +0.52km | 폰 종료 명령이 워치에 안 감 → 워치 워크아웃 생존 → 누적 거리 이월 | `cd3b0d2` `149c4cc` |
| 유령 기록 8건(0.01~0.45km, 최소 3초) | 저장 관문에 거리 하한만 있고 속도 상한이 없었다 | `5f99510` |
| 워치 러닝 3.52km 따로 저장 | 위와 **한 뿌리**(정지 미도착) | `149c4cc` |
| 갤럭시 케이던스 1 spm | expo-sensors 가 백그라운드에서 센서 구독을 뗀다 | `fa39820` |
| 워치 페이스 널뜀 | 계단식 HK 거리를 짧은 창으로 미분 | `fa39820` |
| 갤럭시 거리 −5.7% | **미확정** — 08-10 도플러 전환이 유력하나 증거 부족 | `8a2b4e7`(되돌림+진단) |

**갤럭시 심박 `--` 는 결함이 아니다** — 이 폰(안드로이드 12)에 Health Connect 가 없고,
애초에 심박을 잴 기기(워치·밴드)가 없었다. 가민은 아이폰에 물려 있었다.

#### 아직 확정 못 한 것 — 다음 러닝에서 판가름

갤럭시 −5.7% 의 원인이 도플러 전환인지 **모른다**. 근거는 시점(08-10 변경)과
부호(과대→과소)뿐이고 정황이다. 안드로이드 `Location.getSpeed()` 는 값이 없으면 0 을
주고 우리 코드는 그걸 '모름'으로 읽어 위치 경로로 폴백하므로, **갤럭시가 애초에 도플러를
안 썼을 가능성**도 있다.

그래서 저장 시 두 거리를 나란히 남기게 했다(`distdiag_<id>`, 기록 상세 한 줄):

```
거리 진단 · 사용 X / 속도적분 Y / 위치 Z km
```

다음 러닝에서 이 세 값을 가민과 나란히 적으면 확정된다. **확정되면 이 줄은 지운다.**

#### 이 측정이 남긴 규칙

07-13 줄에 이미 적혀 있던 교훈이 다시 확인됐다 — **한 번의 측정으로 결론 내리면 안 된다.**
08-05 에 "안드로이드 거리 오차 0%, 안 손봐도 된다"고 적었는데, 그건 **거리만** 본 것이었고
같은 날 케이던스는 0 이었다. 그래서 `docs/device-verification.md` 를 만들어
**지표별로** 칸을 세웠다. 하나라도 비면 "검증 완료"라고 쓰지 않는다.


---

## 새 실측을 할 때

1. **동시에** 들고 뛴다. 다른 날 다른 코스 숫자는 비교가 안 된다.
2. 적을 것: `기기별 거리 / 고도 / 케이던스 / 심박` + **코스 종류**(평지·시내 신호등·터널)
   + 화면을 켰는지 껐는지(주머니 러닝 여부).
3. 종료를 누르는 **그 순간 화면을 사진**으로 남긴다 — 저장 후 값과 다를 수 있고,
   그 차이 자체가 데이터다(폰↔워치 병합).
4. 여기 적고, 그 값이 상수를 정했으면 **그 상수 주석에도** 적는다.
5. 회귀로 고정할 수 있으면 `__tests__/lib/gpsAccuracy.real.test.ts` 에 넣는다.
