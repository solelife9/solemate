# ✅ 감사 완료 체크리스트 — 민우님 확인용

> 갱신 2026-07-31 · 마지막 커밋 `2323525`
> **읽는 법:** ✅ 제가 끝냈습니다 · 🔵 민우님이 하셔야 합니다 · 🟡 결정이 필요합니다
> 게이트 상태: `tsc` 0 · `lint` 0 에러 · **254스위트 2615테스트** · 규칙 71건 — 전부 그린

---

## 1. 자리를 비우신 동안 한 것 (AUDIT 1 — 보안)

### ✅ Firestore 규칙 — 공격 35건을 실제로 돌려봤습니다

규칙을 눈으로 읽고 "안전할 것 같다"고 하지 않았습니다. **에뮬레이터에 진짜 규칙을 올려
공격자 계정으로 직접 찔러봤습니다.** 35건 전부 막혔습니다.

- 남의 러닝 기록 읽기 → 불가능
- 남의 신발·기록 수정·삭제 → 불가능
- 신발 카탈로그 변조 → 불가능
- 로그인 없이 읽기 → 불가능
- 남의 uid 로 문서 만들기 → 불가능
- 최근 추가한 컬렉션이 열려 있나 → 아니오

**재현:** `npm run test:rules`

### ✅ 🔴 심각한 결함 1건 발견 → 막았습니다

**기기를 같이 쓰면 앞사람 기록이 뒷사람 계정으로 올라갔습니다.**

```
A 로그인 → 사용 → A 로그아웃 → B 로그인
   → A 의 신발·러닝이 B 의 클라우드 계정으로 병합·업로드
   → B 가 A 의 GPS 이동 경로까지 봅니다. 앱 재시작도 필요 없습니다.
```

원인은 로컬 저장소에 **"이 데이터가 누구 것인지" 표시가 없었던 것**입니다. 로그아웃은
화면만 정리하고 기록은 그대로 두거든요(그건 원래 의도된 설계입니다 — 오프라인 보호).

**막은 방법:** 캐시에 주인을 적어두고, 동기화 직전에 "지금 로그인한 사람이 주인인가"를
확인합니다. 다르면 **업로드도 다운로드도 하지 않습니다.**

**일부러 '삭제'가 아니라 '차단'으로 했습니다.** 로그아웃할 때 기록을 지우는 게 간단하지만,
비행기모드에서 달린 기록이 아직 클라우드에 못 올라갔다면 그게 날아갑니다.

### ✅ 비밀값 — 유출 없음

- **git 커밋 1,147개 전수 검사** → 과거에 올렸다 지운 비밀값 **0건**
- 저장소에 있는 키 3개는 전부 **원래 공개되는 종류**(앱에 박혀 배포되는 클라이언트 키)
- 다만 **`.gitignore` 에 `*.p8`·`*.p12`·`*.mobileprovision` 이 빠져 있었습니다** → 추가했습니다.
  `.p8` 은 애플 푸시 인증 키로 **진짜 비밀값**입니다. 나중에 푸시 설정하면 받게 되는데,
  그때 실수로 커밋되지 않게 미리 막아뒀습니다.

### ✅ 소셜 로그인 — 설계가 옳습니다

가장 흔한 취약점은 "앱이 준 토큰을 서버가 그냥 믿는 것"입니다. 그러면 남이 자기 앱에서
만든 카카오 토큰으로 우리 계정을 만들 수 있습니다. **keego 는 서버에서 검증합니다** —
카카오는 앱 ID 대조, 네이버는 우리 앱 자격으로 토큰 교환. 환경변수가 없으면 아예 거부합니다.

---

## 2. 🔵 민우님이 하셔야 하는 것 (제가 못 합니다)

| # | 할 일 | 왜 |
|---|---|---|
| 1 | **Google Maps 키 제한** — Cloud Console → 이 키에 패키지명 `com.solemate` + SHA-1 `39:D4:...:67:CE` 제한 + Maps SDK 만 허용 | 제한이 없으면 **아무나 이 키로 지도를 써서 민우님께 과금**됩니다 |
| 2 | **App Check enforce 켜기** — Firebase 콘솔 | 지금은 앱에 붙어만 있고 실제 차단은 꺼져 있습니다. 켜야 남용 방어가 작동합니다 |
| 3 | **Firebase 예산 알림** 설정 | 이상 과금을 조기에 알 수 있습니다 |
| 4 | **위치기반서비스사업 신고**(방통위) | **수 주 걸립니다. 출시일을 좌우합니다.** 아직 안 하셨다면 이게 제일 급합니다 |
| 5 | **`aps-environment` → production** 확인 (Xcode 실빌드) | iOS 푸시 |
| 6 | **Play 재서명 지문 등록** — 첫 업로드 후 Play Console에서 확인 → Firebase·카카오에 추가 | 빠뜨리면 **스토어 앱에서만 로그인 전멸**. 내부 테스트로는 재현 안 됩니다 |
| 7 | **실기기 러닝** (아이폰 + 갤럭시) | 거리 정확도를 **안드로이드는 한 번도 안 재봤습니다**. 아이폰 튜닝값도 미검증 |
| 8 | **위젯 홈 화면 배치 확인** | 에뮬레이터 자동화로는 못 얹었습니다. 위젯 목록 미리보기까지는 정상 확인 |

---

## 3. 🟡 결정해 주셔야 진행합니다

| # | 사안 | 선택지 |
|---|---|---|
| 1 | **계정별 저장소 완전 분리** (위 결함의 나머지 절반) | 지금은 "클라우드 오염"만 막았습니다. **B 가 화면에서 A 기록을 보는 것**까지 막으려면 저장소 구조를 계정별로 나눠야 합니다. 데이터 안전이 걸려 승인이 필요합니다 |
| 2 | **탈퇴해도 검색 기록이 남습니다** | `search_misses`·`shoe_requests` 에 계정 식별자가 함께 저장되는데, 규칙상 앱이 **찾을 수도 지울 수도 없습니다**. 처리방침("탈퇴 시까지")과 어긋납니다.<br>**권장: 애초에 계정 식별자를 저장하지 않기**(신호 목적상 불필요) |
| 3 | 사진 클라우드 미백업 | 재설치하면 영구 소실(화면이 고지는 함) |
| 4 | 앱 ID `com.solemate` vs 브랜드 Keego | **출시 후 영구 고정**. 지금이 마지막 기회 |
| 5 | 신발 카탈로그 서버 업로드 | 전수조사 끝나면 |
| 6 | 스펙 소스 통일 | 충돌 36건 판정 후 (`docs/audit/spec-source-conflicts.md`) |

---

## 4. 오늘 전체 작업 요약 (AUDIT 0 이후)

<details>
<summary>펼쳐보기</summary>

| 커밋 | 내용 |
|---|---|
| `767032e` | 리더보드가 동의 없이 개인정보를 공개 컬렉션에 쌓던 것 차단 + 전량 삭제 |
| `8f4ef86` | 백업 문서 1MiB 폭탄 — GPS 경로를 사이드카로 분리 |
| `22b23de` | flaky 테스트 원인 규명 + 전역 차단 |
| `923e182` | 알림 카피를 실제 동작에 맞춤(오지 않는 알림 약속 제거) |
| `8a45f59` | 신발 스펙 423켤레가 '다음 신발'에 미연결이던 것 연결 |
| `83c7dbd` | lint 시한폭탄(여유 1개) 해소 |
| `535eb25` | 출시 서명 조용한 폴백 차단 |
| `cb0c07e` | **안드로이드가 3주 낡은 코드로 돌던 것** 발견·수정 + 재발 방지 |
| `0b39f63` | 로그인 전 알림 차단 + 안드로이드 출시 키 지문 반영 |
| `a5d60e3` | **안드로이드 홈 위젯** (iOS 동등) |
| `2323525` | **AUDIT 1 보안** — 공격 35건 실증 + 계정 전환 오염 차단 |

**만든 감사 문서**
- `docs/audit/00-baseline.md` — 기준선
- `docs/audit/01-security.md` — 보안 (AUDIT 1)
- `docs/audit/01-store-review-risk.md` — 스토어 심사 위험
- `docs/audit/spec-source-conflicts.md` — 스펙 충돌 36건

</details>

---

## 5. 제가 이번 감사에서 틀렸던 것 (기록)

같은 실수를 반복하지 않으려고 남깁니다. **전부 "문서·주석만 보고 코드를 안 열어봐서"** 났습니다.

| 처음 판정 | 실제 |
|---|---|
| "테스트 red — Iron Law 위반" | flaky 였음. 연속 5회 그린 |
| "OS 스케줄 알림 없음" | 러닝 리마인더는 **실제로 동작**함 |
| "스펙 커버리지가 낮다" | 커버리지가 아니라 **연결이 안 된 것**이었음 |
| "출시 빌드가 debug 키로 서명됐을 것" | 정식 키로 서명돼 있었음 |
| "워치 코드 8,600줄" | 빌드 산출물 포함. 실제 2,987줄 |
| "카카오 버튼 글씨가 안 나온다" | 제가 쓴 `monkey` 명령이 버튼을 눌러 스피너가 뜬 것 |
| "공격 71건" | 71 은 두 스위트 합계. 공격은 **35건** |

---

## 다음 AUDIT

준비해 두신 순서대로 주시면 이어서 진행하겠습니다.
**`[공통 꼬리말 붙이기]`** 가 프롬프트에 있었는데 그 꼬리말 내용을 아직 못 받았습니다 —
알려주시면 이 문서와 `01-security.md` 에 소급 적용하겠습니다.
