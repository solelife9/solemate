# 08 — 6축 병렬 재감사 + 수정 (2026-08-07)

> 작성 2026-08-07 · 기준 커밋 `bfcf6ee` · **코드 수정 있음**(아래 커밋 18건)
> 선행 감사: `docs/audit/00-baseline.md` ~ `07-launch-ops.md`(2026-07-29 ~ 08-04) · `FINAL-readiness.md`(08-06)
>
> **이 문서를 쓰는 이유부터.** 이 감사는 세션 안에서만 존재했고 결과가 저장소 밖(아티팩트)에
> 있었다. 그래서 정본 잔여 목록(`FINAL-readiness.md` · `TODO.md`)에 여기 내용이 빠졌고,
> **프로젝트에 잔여 목록이 두 개** 생겼다. 그 상태에서는 출시 준비도 점수도 남은 시간 추정도
> 성립하지 않는다. 옆 세션의 지적이 맞아 문서로 올린다.

---

## 이 감사가 선행 감사와 다른 점

선행 감사 11개는 **코드 읽기**였다. 이번엔 여섯 축을 병렬로 돌리면서 아래를 추가했다.

| 추가한 것 | 그래서 나온 것 |
|---|---|
| **`node_modules` 라이브러리 원본 읽기** (선행 감사 0건) | 배경 위치 권한 불필요(expo-location 주석에 명시) · 기압계 권한 무음 실패 · 알림 폴백 채널 |
| **머지된 매니페스트 검사** (선행 감사 0건) | 광고 식별자 수집 · 불필요 권한 14개 유입 |
| **엔진 직접 실행**(스크래치 하네스) | 死구간 페이스 `0'52"/km` · 워치 고도 1,843m |
| **"이 테스트가 무엇을 목으로 대체하는가"** 를 물음 | jest 목이 `merge` 옵션을 무시 · 컬렉션 참조에 throw — 그 탓에 `runDetails` 파기가 **테스트에서 한 번도 실행된 적 없었다** |

> ⚠️ 선행 감사들은 **"테스트 3,207개 그린"을 안전의 근거로 인용했다**(문서당 1~13회).
> 그런데 그 스위트는 기압계·안드로이드 경로·I/O 계층을 구조적으로 못 본다.
> 초록불을 근거로 쓰는 습관이 11번 반복됐다.

---

## 왜 지금 나왔나 — 선행 감사가 부실했던 게 아니다

| 사실 | 수치 |
|---|---|
| 선행 감사 11개 작성 기간 | 2026-07-29 ~ 08-04 |
| 그 이후 main 에 들어온 커밋 | **61건** |
| 오늘 고친 코드의 파일 생성일 | `healthConnect.ts` 08-06 · `health.ts` 08-06 · `runNotification.ts` 08-05 · `recordSync.ts` 08-01 |

**가장 뼈아픈 구조적 원인** — 감사가 지적한 것을 고친 **그 수정을 아무도 다시 감사하지 않았다**:

```
08-01 00:42  데이터 무결성 감사 종료 — "묘비가 쌓여 1MiB, 동기가 멎는다"
08-01 10:15  recordSync.ts 커밋 — 그 지적의 해결책          ← 9시간 33분 뒤
```

그 해결책이 오늘 찾은 블로커 **3개**를 한꺼번에 만들었다(탈퇴 미삭제 · 묘비 `merge:true` ·
계정 커서 격리 누락). 이 프로젝트는 감사를 11번 돌렸지만 **감사가 만든 수정을 감사한 적은
한 번도 없다.**

**권고:** 다음 감사 대상은 "코드 전체"가 아니라 `git log <마지막 감사>..HEAD` 다.

---

## [1] 수정 완료 (커밋 18건)

### 되돌릴 수 없는 것 — 6/6 종결

| # | 결함 | 커밋 |
|---|---|---|
| A | **탈퇴해도 러닝 기록 전량이 남는다.** `runs`·`shoes`·`medals` 하위 컬렉션이 파기 목록에서 빠졌다(08-01 recordSync 도입 때). 계정이 사라져 **본인도 못 지운다** | `2e7daaf` |
| B | **삭제한 러닝의 본문이 영구 잔존.** 묘비 껍데기화 로직은 옳고 테스트도 통과했는데, I/O 계층의 `merge:true` 가 그 의도를 지웠다 | `2e7daaf` |
| C | **공개 동의가 계정 전환 시 상속.** `social_visibility_v1` 이 격리 목록에 없고 마운트 1회만 읽는다 → B 가 동의한 적 없이 발행 | `31345ca` |
| D | **동의를 철회해도 리더보드에서 안 내려간다.** `unpublish` 의 유일한 호출부가 가드 뒤라 **도달 불가능한 코드**였다 | `31345ca` |
| E | **워치가 먼저 도착하면 신발 이중 차감.** 병합 검사가 워치 수신 경로에만 있었다 | `cc241eb` |
| F | **탈퇴가 다른 계정의 보관함까지 삭제.** `AsyncStorage.clear()` | `4c2686d` |

함께 격리한 계정 키 6개: `recordSync_cursor_v1`(누락 시 **"내 기록이 다 사라졌다"**) ·
`recordSync_pushed_v1` · `watch_runs_seen_v1` · `detail_delete_pending_v1` ·
`hr_recover_sweep_at_v2` · `hr_repair_done_v1`

### 순이익

| # | 내용 | 커밋 |
|---|---|---|
| G | **`ACCESS_BACKGROUND_LOCATION` 제거.** expo-location 원본이 "포그라운드 서비스면 이 권한 불필요"라고 명시하고, 이 앱은 항상 그렇게 시작한다. **Play 배경 위치 선언 + 시연 영상 제출이 사라진다.** 잃는 것 없음 | `3c7571c` |

### 정확성

| # | 결함 | 커밋 |
|---|---|---|
| H | **안드로이드에서 화면을 끄면 거리가 죽는다.** 08-05 케이던스 수정의 부작용 — 얼어붙은 걸음 값에 현재 시각을 찍어 먹여 게이트가 "서 있음"으로 판정. **6'40"/km 보다 느린 러너 전부** | `b892847` |
| I | **死구간에서 거리는 인정하고 시간은 지웠다.** 실측: 60초 + 5분 터널 → 페이스 **0'52"/km**. 터널 하나가 가짜 PB를 만든다 | `9e1c7de` |
| J | **워치 런 고도에 상한이 아예 없다.** 400표본 시뮬 최대 **1,843m** — 08-05 아이폰 1,814m 의 쌍둥이 | `6f74787` |
| K | **수직 정확도를 한 번도 안 봤다.** RawFix 에 필드조차 없었다. 워치 Swift 는 이미 보고 있었다 | `2506921` |
| L | **시작 시각을 역산하고 있었다.** `updatedAt − duration` — 저장 시각 − 이동 시간이라 일시정지만큼 밀린다. 심박 창·48h 스윕(정확한 트랙을 덮어씀)·병합 창 셋을 동시에 오염 | `21be3c0` |
| M | **평균 케이던스의 분자/분모 불일치.** 일시정지 중 걸음이 분자에 포함 → 200 spm 대 저장. 복구 런은 반대로 24 spm | `bfcf6ee` |
| N | 일시정지 이동 넛지가 안드로이드에서 영구 사망(08-05 커밋이 두 호출부 중 하나만 고침) | `b892847` |

### 안드로이드 · UX

| # | 결함 | 커밋 |
|---|---|---|
| O | 알림이 **폴백 채널로** 뜬다(`channelId` 미전달) — 소리·진동·중요도 설정 전부 무시 | `4b7a5f9` |
| P | 안드로이드에서 **빈 공유 시트를 열고 성공을 반환** — GPX 내보내기 · 은퇴 카드 공유 · 은퇴 카드 저장 | `0d979db` |
| Q | 공유 카드 배율 — 08-06 스윕에서 **가장 많이 쓰는 카드**(러닝 리캡)와 키프세이크가 빠졌다. 키프세이크는 미리보기 해상도로 구워져 **설계의 36%** | `6e239c6` |
| R | 축하 화면에 **갇힌다** — 스크롤 없음 + 문구 길이 제한 없음 + 등급 상승 변형엔 건너뛰기 없음 | `51cd368` |
| S | **VoiceOver 로 '실행취소'에 닿을 수 없다** — 파괴적 액션의 유일한 복구 경로 | `65535d7` |
| T | `PingRing` 이 reduce-motion 무시 + 링이 메달보다 작아 대부분 가려짐 | `51cd368` |
| U | 08-06 커밋이 놓친 'Apple 건강' 문구 2줄 | `4b7a5f9` |

### 신설한 회귀 가드 — **전수 스윕 3건**

개별 수정만으로는 "다음 파일이 또 빠지는" 패턴을 못 막는다. 그래서 목록이 아니라
**디렉터리를 긁어** 검사하는 가드를 세웠다.

| 가드 | 무엇을 막나 | 파일 |
|---|---|---|
| 공유 카드 배율 | 새 카드가 규약을 빠뜨림 | `__tests__/shareCardCaptureScale.test.ts` |
| 저장소 키 분류 | 새 키가 계정 격리에서 누락(위 C·6개를 실제로 찾아냄) | `__tests__/lib/accountScope.test.ts` |
| 건강 앱 이름 | 화면에 플랫폼 이름 하드코딩 | `__tests__/ProfileScreen.health.test.tsx` |

그 밖: 배경 위치 권한 **역방향** 가드 · 탈퇴 하위 컬렉션 · 묘비 I/O 계약 ·
안드로이드 걸음 게이트 · 도착 순서 무관성 · `runWindow` 우선순위 · 케이던스 불변식 ·
안드로이드 공유 경로(`Platform.OS` 를 명시적으로 바꿔 검사).

**그리고 jest 목 자체를 고쳤다** — `merge` 옵션을 존중하게, 컬렉션 참조를 받게.
목이 실제보다 관대하면 그 위의 테스트는 아무것도 지키지 못한다.

게이트: `tsc` 0 · `lint` 0 에러 · **294 스위트 3,290 테스트** 그린(3,207 → +83).

---

## [2] 남은 것 — 코드

### ⛔ 네이티브 (Danger Zone · 민우님 승인 필요)

| # | 항목 | 왜 안 했나 |
|---|---|---|
| N-1 | **iOS 홈 위젯이 가짜 신발을 보여준다.** 확장 타깃에 App Group entitlement 가 없어 `.sample` 폴백 → **모든 사용자 홈 화면에 "Nike Pegasus 41 · 118/650km"**. Truth only 정면 위반. *(안드로이드 위젯은 같은 상황에서 "러닝화를 등록해 주세요"를 띄운다 — iOS 만 반대)* | Xcode capability + Apple 개발자 포털 등록 필요 |
| N-2 | **폰이 '이번 러닝의 신발'을 워치에 안 알려준다.** 워치는 마지막 스와이프 신발로 세션을 연다 → 신발이 다르면 병합 조건(`shoe_id` 동일)이 깨져 위 E 수정이 무력화 | Swift 양쪽 수정, 빌드 검증 불가 |
| N-3 | 워치 콜드런치 버퍼 재생 순서(`runs → stops`)가 중복을 만든다 | Swift 1줄 |
| N-4 | **안드로이드 실내 러닝 = 항상 0.00km.** 거리 정본 모듈이 iOS 전용 | 안드로이드 만보계 거리 경로 신설 필요 |

### 🟡 보류 — 실측 데이터가 있어야 정할 수 있다

**고도 상한의 잡음 대역(30~120초).** 감사 초기 판정은 "상한을 조이면 된다"였는데,
**직접 재보고 그 판정을 철회했다:**

| 시도 | 결과 |
|---|---|
| 90초 이동평균 | 평지 잡음은 전멸 → 그런데 **진짜 언덕 반복(10m×20)이 200m → 6m** |
| 임계 10m + 무평활 | 합성 정현파에선 완벽 → 큰 진폭 저주파에서 오히려 악화 |
| 랜덤워크(OU) 모델 24시드 | **현행 규칙이 진짜 200m 등반을 27m 로 과소 보고**(정반대 실패) |

결과가 잡음 모델에 심하게 의존한다. 이 상태로 Danger Zone 정확도 상수를 바꾸는 건
이 프로젝트가 금지하는 추측이다. **실기기 원자료를 받은 뒤 정한다.**
(그 사이 추측이 필요 없는 부분 — OS 가 무효라고 표시한 표본 버리기 — 만 K 로 넣었다.)

### 🔵 남은 코드 작업 (제가 할 수 있는 것)

정확성 잔여(GAP 이 기압계 대신 GPS 고도 사용 · 스냅샷에 고도/hrTrack 누락 · Health Connect
부분겹침 레코드 · 무음 실패 9곳 · 심박 목표존 플랫폼 게이트 · 평균/존 가중 불일치 ·
maxHr 미저장으로 TRIMP 미발동 · `editRun` 칼로리 미재계산 · 중복 저장 3경로) ·
안드로이드(폰트 700/800 폴백 · `BackHandler` · edge-to-edge · 스플래시 · 햅틱 · 알림 아이콘 ·
불필요 권한 14개 · R8) · 스토어 법무(광고 ID · 처리방침 미고지 6건 · UGC 신고/차단 ·
Health Connect 근거 화면 · 14세 확인 순서) · 비용(리더보드 무캐시 — 무료 한도 **~89명**) ·
UX 잔여.

---

## [2-B] 출시 범위로 승격 — 구 '1.1 나중에' 목록 (2026-08-07 민우님 결정)

이 문서 초판(과 작업 순서표)은 아래를 **"지금 하면 손해"** 로 분류해 1.1 로 미뤘다.
근거는 ① 출시 전 수정은 재검증 비용(스크린샷·QA·CI)이 붙고 ② 사용자 0명일 때의 우선순위
판단은 대부분 틀리며 ③ 첫 코호트는 다시 오지 않는다는 것이었다.

**민우님이 그 판단을 뒤집었다 — Wear OS 앱 하나만 남기고 전부 출시 전에 한다.**
그 결정을 존중해 여기 옮겨 적는다. 아래는 이제 **출시 블로커와 같은 취급**이다.

| # | 항목 | 왜 미뤘었나 → 지금 하는 이유 | 담당 | 규모 |
|---|---|---|---|---|
| L-1 | **워치 컴플리케이션** | 없어도 워치 앱은 완전히 동작한다. 다만 **프리미엄 체감으로는 가장 큰 단일 갭** — 워치 페이스에 링이 박혀 있는 것이 매일의 브랜드 접점이고, NRC·Strava·Apple 은 전부 있다 | 네이티브(새 Xcode 타깃) ⚠️승인 | 3~4h |
| L-2 | **iOS 잠금화면 위젯** | 홈 위젯만 있다. 문서·주석은 "홈/잠금"이라 적혀 있어 **문서가 코드보다 앞서 있었다** | 네이티브 | 1.5h |
| L-3 | **안드로이드 위젯 — 딥링크 · 미디엄 크기** | 지금은 탭하면 앱만 열린다(iOS 는 `keego://start` 로 바로 러닝 시작). **위젯의 핵심 가치가 안드로이드엔 없다.** 리사이즈도 막혀 있다(`resizeMode="none"`) | 옆 세션(Android) | 1.5h |
| L-4 | **워치 실내(트레드밀) 러닝 · AOD 전 지표** | 워치가 `.outdoor` 하드코딩이라 트레드밀에서 GPS 를 헛돌려 배터리만 태운다. AOD 는 **경과시간만 살아 있고 거리·페이스는 옛 숫자** — 손목 내리고 달리다 흘끗 보면 멈춘 값이 보인다 | 네이티브(Swift) | 4h |
| L-5 | **안드로이드 만보계 거리(CMPedometer 대응물)** | 터널·도심 협곡 GPS 사각지대 거리 복구가 안드로이드엔 없다 — 기능 갭이 아니라 **품질 갭**이다. 실내 러닝 0km(§2 N-4)와 **같은 모듈**이라 함께 간다 | 옆 세션(Android) | — |
| L-6 | **사진 클라우드 백업** | 지금은 재설치는 물론 **저장공간 압박만으로도** iOS 가 조용히 지운다(`Library/Caches` 에 산다). 화면 고지 문구도 "앱을 지우면"이라 부정확 | 나 + ⚠️**민우님 결정**(스토리지 비용·용량 정책) | — |
| L-7 | **랭킹 점수 서버 재계산** | 지금은 클라이언트가 계산해 자기 점수를 부풀릴 수 있다. 순위를 상금·보상과 연결하지 않는 한 피해는 없지만, **출시 후에 고치면 이미 오염된 리더보드를 들고 시작**한다 | 나(Cloud Functions) ⚠️배포 필요 | — |
| L-8 | **신발 스펙 28켤레 · 카탈로그 결손** | 빈칸은 정직하지만 **shoe-first 가 이 앱의 차별점**이다. 차별점의 밀도가 곧 프리미엄감 | 옆 세션(데이터) | 3~5h |
| L-9 | **알림 액션 버튼(일시정지·종료)** | 잠금 상태에서 자동 일시정지를 봐도 해제하려면 잠금을 풀어야 한다 — **잠금화면 알림의 목적을 반쯤 무효화**한다. NRC·Strava 안드로이드판은 알림에서 바로 조작된다 | 옆 세션(Android) | 3~4h |
| L-10 | **안드로이드 리플 · borderCurve** | `android_ripple` 0건 · `borderCurve:'continuous'` 97곳이 iOS 전용이라 안드로이드는 평범한 코너 — 재질감 손실 | ⚠️**디자인 결정 먼저** | — |
| L-11 | **`App.tsx` 분해** | 아키텍처 부채. ⚠️ **내 정확성 작업이 끝난 뒤에** — 파일 전체를 다시 쓰는 작업이라 동시 편집이면 한쪽이 통째로 사라진다 | 옆 세션(내 작업 종료 후) | — |
| L-12 | **lint 경고 170 · `substr` 1건 · 루트 잔여 파일** | 위생. 0에러이고 상한 209 대비 여유 39 | 나 | — |
| L-13 | **Node 22 에서 테스트가 매달린다** | 개발·CI 모두 26 이라 **아무도 안 밟는다.** 밟는 순간은 남에게 코드를 넘길 때 | 나 | — |

### 여전히 1.1 — 유일한 예외

| 항목 | 왜 |
|---|---|
| **Wear OS 앱** | 스크래치부터 Compose for Wear OS + Health Services. **최소 2~3주** — 워치 앱은 3,159줄짜리 독립 제품이라 '항목'이 아니라 별도 프로젝트다. ⚠️ 다만 **Play 등록정보에서 "Apple Watch 단독 러닝"을 빼는 것은 출시 블로커**다(§2 참조) — 없는 기능을 광고하면 등록정보 위반 소지 |

---

## [3] 정본 문서에 반영할 것

- `FINAL-readiness.md` §5 의 "출시 후에 해도 되는 것"에 **위 [2] 남은 것**을 병합해야 한다.
- 위 A~U 는 **이미 수정됐으므로** 잔여 목록에서 빼야 한다.
- `FINAL-readiness.md` §1.5 안정성 점수의 근거였던 "실기기 잔여 6건"은 유효하나,
  이번 수정으로 **검증해야 할 항목이 늘었다**(아래).

### 실기기 검증 추가 항목 (이번 수정분)

1. **안드로이드 · 화면 끄고 주머니 · 7'00"/km 10분+** — H 검증. 최우선.
2. **아이폰 · '동작 및 피트니스' 켠 상태 야외 30분+** — 기압계 고도가 실제로 맞는지.
   *이 조건의 실측 데이터가 지금까지 하나도 없다.*
3. **폰+워치 동시, 워치에서 다른 신발을 고른 뒤 시작** — E 수정 확인 + N-2 재현.
4. 최대 글꼴 + 작은 화면에서 등급 상승 축하가 스크롤되는지 — R 검증.
5. 안드로이드 공유(GPX·은퇴 카드) 가 실제로 파일을 붙이는지 — P 검증.

---

*작성: Claude Code(터미널 세션) · 6축 병렬 감사 — 안드로이드 패리티 · 러닝 지표 정확성 ·
데이터 안전 · UX/브랜드 · 스토어/법무 · 워치/플랫폼*
*모든 수정은 커밋마다 `tsc` · `lint` · 전체 스위트를 통과시켰다.*
