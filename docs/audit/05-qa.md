> 2026-07-29 재감사. 선행 감사: .tenet/spec/ 의 3개 감사 문서 참조.

# 05 — QA 시나리오 예측 (20건)

> 작성 2026-08-04 · 기준 = 현재 워킹트리(main, `957508f` + 미커밋 2파일)
> **코드 수정 0 — 읽기만 했다.**
> 판정은 **오직 코드**로 한다. 코드만으로 결론이 안 나는 것(센서·OS 스케줄러·실측 시간)은
> "실기기 필요"로 분리했다.
> `docs/audit/00-baseline.md` 에서 **[해결됨]** 으로 판정된 항목은 여기 넣지 않았다.
> (예: 백그라운드 트래킹 부재·크래시 스냅샷 부재·pausedMs 폭주·RDP 경로 단순화 —
> 마지막 것은 baseline 당시 `MINOR` 미해결이었으나 `lib/geo.ts:80-140` 에서 이미 RDP 로 교체됐다.)
> 심각도: `BLOCKER` 출시 불가 · `MAJOR` 이탈/나쁜 리뷰 · `MINOR` 출시 후 · `NITPICK` 취향

---

> **처리 현황(2026-08-04 갱신).** BLOCKER 1건 + MAJOR 4건 **수정 완료**(커밋 5건,
> `a2a3ddc`·`4d2d109`·`5516be0`·`91e0776`·`aaf792b`). 이어서 민우님 지적으로
> **위치 권한 안내 시점**과 **실내 러닝 차단**도 고쳤다(`9425376` — §6 참조).
> **MINOR 6건(Q-6 ~ Q-12)도 전부 수정 완료**(2026-08-04 — §6 참조).
> 게이트 전부 통과 — `tsc` 0 에러 · `lint` 0 에러(경고 238/상한 241) ·
> `test` 283 스위트 3,142개 전량 통과.
> 아래 본문은 **발견 당시 기록**이며, 수정된 항목에는 `✅ 수정됨` 을 달았다.
> **남은 것은 §2 의 실기기 항목 8건뿐이다** — 코드로 대체할 수 없다.

## 0. BLOCKER 요약 (먼저 볼 것)

| # | 시나리오 | 무슨 일이 일어나는가 | 근거 | 상태 |
|---|---|---|---|---|
| **Q-1** | [3] 위치 권한만 거부하고 러닝 시작 | 권한 거부로 엔진이 **시작되지 않은 채** 종료를 누르면, 종료 경로가 싱글턴 엔진에 남아 있는 **직전 러닝의 거리·시간·경로를 그대로 읽어 새 기록으로 저장**한다. 같은 러닝이 두 건이 되고 신발 거리가 두 번 차감된다(Iron Law: 데이터 유실·오염 금지 위반) | `screens/RunEngine.tsx:404-408` · `:731` · `lib/runTracker.ts:226-289` | **✅ 수정됨** `a2a3ddc` |

BLOCKER는 1건이다. 나머지는 아래 표.

---

## 1. 실패 예상 항목 (표)

| # | 시나리오 | 판정 | 결함 | 심각도 | 상태 |
|---|---|---|---|---|---|
| 3 | 위치 권한만 거부하고 러닝 시작 | **실패 예상** | Q-1 직전 러닝 복제 저장 | `BLOCKER` | **✅ `a2a3ddc`** |
| 8 | 러닝 중 비행기모드 on/off | **실패 예상** | Q-2 오프라인에서 **회원 탈퇴가 영구 대기**(무반응) | `MAJOR` | **✅ `aaf792b`** |
| 8 | 〃 | **실패 예상** | Q-3 완주 저장이 **타임아웃 없는 역지오코딩을 await** | `MAJOR` | **✅ `91e0776`** |
| 20 | 러닝 기록 1000개 통계·목록 | **실패 예상** | Q-4 `runs` 변경마다 **런 수만큼 개별 AsyncStorage getItem**(주석은 multiGet 이라 적혀 있다) | `MAJOR` | **✅ `5516be0`** |
| 9 | 강제 종료 → 재실행 → 이어 달리기 | **부분 실패** | Q-5 복구 다이얼로그가 **로그인/부팅 게이트 위에** 뜬다 | `MAJOR` | **✅ `4d2d109`** |
| 12 | 자정을 넘기는 러닝 | **부분 실패** | Q-6 **종료 시각의 날짜**로 저장 → 스트릭·주간 목표 어긋남 | `MINOR` | **✅ `d79293a`** |
| 2 | 모든 권한 거부 상태에서 앱 사용 | **부분 실패** | Q-7 카메라/앨범 거부 시 **무반응**인 진입점 3곳 | `MINOR` | **✅ `407a0bf`** |
| 16 | 같은 신발 중복 등록 | **실패 예상** | Q-8 중복 방어가 **전혀 없다** + 화면에서 구분 불가 | `MINOR` | **✅ `25336ca`** |
| 18 | 긴 이름 / 이모지 / 특수문자 | **실패 예상** | Q-9 입력 길이 제한 0 + 홈 히어로 브랜드 줄에 `numberOfLines` 없음 | `MINOR` | **✅ `83e2fc5`** |
| 20 | 러닝 기록 1000개 | **실패 예상** | Q-12 매 렌더 O(신발×런) 파생 계산이 메모 없이 돈다 | `MINOR` | **✅ `83e2fc5`** |
| 15 | 신발 삭제 후 그 신발의 러닝 기록 | **부분 실패** | Q-10 목록은 이름을 버리고 상세는 살린다(불일치) + 90일 뒤 소실 | `MINOR` | **✅ `25336ca`**(90일 TTL 은 별건) |
| 12 | 수동 기록 폼 (부수 발견) | **부분 실패** | Q-11 **보관(retired)한 신발이 신발 칩에 그대로 뜬다** | `MINOR` | **✅ `25336ca`** |
| 4 | 로그인 없이 앱 둘러보기 | **실패 예상** | 하드 게이트 — **기존 감사 3-6 로 이미 기록됨**(새 발견 아님) | — | 별건 |

---

## 2. "실기기 필요" 항목 (코드만으로 판정 불가)

| # | 시나리오 | 왜 코드로 못 정하나 | 무엇을 봐야 하나 |
|---|---|---|---|
| 6 | 러닝 중 화면 잠금 → 30분 후 해제 | 배선은 완비(`lib/locationService.ts:65-76` 전역 task + `:165-176` `startLocationUpdatesAsync`). 잠금 30분간 iOS 가 실제로 fix 를 계속 배달하는지, JS 컨텍스트가 살아 1초 틱·3초 스냅샷을 계속 도는지는 OS 소관 | 30분 잠금 전/후 거리·경과시간 연속성, 케이던스(`Pedometer` 폴링) 유지 여부 |
| 7 | 러닝 중 백그라운드 → 타 앱 → 복귀 | 위와 동일 + 복귀 시 `AppState 'active'` 가 클라우드 동기·심박 복구를 동시에 트리거한다(`App.tsx:1636-1641`, `:1885`) | 복귀 첫 프레임의 거리/시간 점프 여부, 지도 폴리라인 연속성 |
| 8 | 러닝 중 비행기모드 on/off | iPhone GPS 수신 자체는 비행기모드에서도 살아 있으나 A-GPS 보조가 끊겨 첫 fix·정확도가 달라진다. 코드로 못 잰다 | 비행기모드 구간의 정확도(m)·`stalledMs` 적립량·해제 후 재수렴 시간 |
| 9 | 강제 종료 → 재실행 → 이어 달리기 | 스냅샷 시드는 코드로 확인됨(`RunEngine.tsx:583-598`). GPS 재가동 후 **첫 fix 가 새 앵커가 되는** 규약이 실제 경로에서 자연스러운지는 실측 | 공백 구간 거리 미계상 확인, 재개 후 페이스 '--' 지속 시간 |
| 10 | 러닝 중 위치 권한 회수 → 재허용 | 회수 감지는 `watchPositionAsync` 의 onError 문자열 휴리스틱(`lib/locationService.ts:117-119`)에 달려 있다. OS 가 어떤 문자열을 주는지는 기기마다 다르다 | 회수 시 배너가 실제로 뜨는가, 재허용 후 거리 보존 재개 |
| 12 | 5시간 러닝 | 5시간이면 경로점이 1만 8천 개 규모다. 15초마다 전량 직렬화(`lib/runTracker.ts:38 ROUTE_WRITE_MS`, `runPersistence.ts:199`)하는 비용은 실측해야 한다 | 3·4시간 경과 시점의 프레임 드랍·배터리·스냅샷 쓰기 지연 |
| 19 | 시스템 글꼴 최대 크기 | 상한은 구현돼 있다(`lib/text.tsx:27,29` — 본문 1.5× · 히어로 1.2×, 직접 `react-native` `Text` import 0건). 남는 건 **고정 높이 컨테이너** 3곳이 1.5× 를 견디는지 | `primitives.tsx:819` seg md `height 38` / `:820` seg sm `height 32` / `:1558` `height 50` 에서 글자 잘림 여부 |
| 1 | 신규 설치 첫 로그인 | 메모에 남은 미해결 1건("첫 설치 첫 로그인 두 번 탭")은 코드 근거를 찾지 못했다 — `LoginScreen.rn.tsx:50-64` 의 busy 가드·`App.tsx:483` 구독 어디에도 두 번 탭을 요구하는 경로가 없다. 네이티브 SDK 초기화 타이밍으로 보인다 | 앱 삭제 후 재설치 → 각 제공자 첫 탭 반응 |

---

## 3. 시나리오별 판정 (20건 전부)

### [첫 실행]

**1. 신규 설치 후 최초 실행 — 데이터 0인 상태로 모든 화면 진입 → 통과 예상**
빈 상태가 화면마다 실제로 존재한다: 홈 `HomeScreen.rn.tsx:364-378 EmptyHome`(고스트 카드) · 신발탭
`ShoesScreen.rn.tsx:679-682`(FirstShoeScreen 통째 반환) · 기록 수동추가 `HistoryScreen.rn.tsx:230-231`
"먼저 신발을 등록하세요" · 마이탭 리캡 `ProfileScreen.rn.tsx:879-885 recap-empty`.
`effectiveId` 가 null 이어도 위젯 push 는 `App.tsx:1738` 에서 빈 문자열로 스킵되고, 예측 계산은
`forecastForRaw(null)` → null 로 떨어진다(`App.tsx:1955-1962`). 크래시 경로 없음.
다만 **Q-5**(복구 다이얼로그)와 **Q-8**(중복 등록)이 이 흐름에 얹힌다.

**2. 모든 권한 거부 상태에서 앱 사용 → 부분 실패 (Q-7)**
- 위치: 안내 O(`RunEngine.tsx:405` 설정 딥링크 다이얼로그).
- 모션/걸음: `RunEngine.tsx:623-647` try/catch — 케이던스만 0, 러닝은 계속. 정상.
- 사진(라이브러리, 프로필): 안내 O(`App.tsx:1678-1683` 설정 열기 다이얼로그).
- **카메라·앨범(그 외 경로): 무반응** → Q-7.
- 알림: 프라이밍은 1회만 뜨고 거부해도 마킹돼 다시 조르지 않는다(`App.tsx:833-848`). 정상.
- HealthKit: 전 경로 graceful(`lib/healthkit.ts` 헤더 규약). 정상.

**3. 위치 권한만 거부하고 러닝 시작 → 실패 예상 `BLOCKER` (Q-1)**

**4. 로그인 없이 앱 둘러보기 → 실패 예상 (기존 기록)**
`App.tsx:2283-2286` 이 `authUser===null` 이면 무조건 LoginScreen 이다. 익명 로그인 구현은
`lib/firebaseCloudPort.ts:260-264` 에 살아 있으나 `LoginScreen.rn.tsx` 는 kakao/naver/google/apple
4개만 노출한다. **baseline 3-6 이 이미 `MAJOR` 로 기록한 항목이라 새 발견으로 세지 않는다.**

### [러닝]

**5. 러닝 시작 후 즉시 종료 (0초·0m) → 통과 예상**
`RunEngine.tsx:732-739` 가 `fk<0.01` 에서 '계속 달리기 / 나가기' 다이얼로그로 막는다.
그 사이 앱이 죽어 0.00km 스냅샷이 남아도 다음 부팅의 검토 경로가 `:819-824` 에서 다시 걸러
"거리가 기록되지 않았어요"로 닫는다. 이중 가드가 걸려 있다.

**6. 러닝 중 화면 잠금 → 30분 후 해제 → 실기기 필요**

**7. 러닝 중 백그라운드 → 타 앱 → 복귀 → 실기기 필요**

**8. 러닝 중 비행기모드 on/off → 실패 예상 (Q-2·Q-3) + 실기기 필요**

**9. 러닝 중 앱 강제 종료 → 재실행 → '이어 달리기' → 부분 실패 (Q-5)**
복구 자체의 배선은 온전하다: `lib/runPersistence.ts:210-229 loadSnapshot`(경로 키 분리 + 구버전
하위호환) → `App.tsx:869-899` 3지선다 → `RunEngine.tsx:583-598` 엔진 시드(`t0 = now − elapsed`,
`seedDist`, `lastGood` 는 일부러 비워 공백 가로지르는 허위 거리 차단). 트랙 런 랩 상태도
`RunEngine.tsx:193-206` 에서 복원된다. 결함은 **다이얼로그가 뜨는 자리**다(Q-5).

**10. 러닝 중 위치 권한 회수 → 재허용 → 통과 예상 (감지 자체는 실기기 필요)**
회수: `runTracker.notifyPermissionRevoked()` 가 **시간을 먼저 동결한 뒤** 플래그를 세운다
(`lib/runTracker.ts:404-415`) — 거리와 시간이 같이 멈춘다. 재허용: `RunEngine.tsx:420-446` 이
`AppState 'active'` 에서 권한을 재확인하고 `resumeFromPermissionRevoked()`(`runTracker.ts:425-443`)
로 **설정 다녀온 공백을 pausedMs 로 흡수**해 경과시간이 점프하지 않는다. 설계상 옳다.

**11. GPS 안 잡히는 실내에서 러닝 시작 → 통과 예상**
`RunEngine.tsx:940` 이 `NO_FIX_WARN_SEC` 뒤에 "신호 없음"을 정직하게 띄우고(트랙·실내 모드는 제외),
거리 0 으로 끝나면 5번과 같은 가드에 걸린다. 사용자가 '실내'를 고른 경우는 GPS 를 아예 켜지 않고
만보계를 거리 정본으로 쓴다(`runTracker.ts:777-786`) — 실내 경로는 별도로 닫혀 있다.

**12. 자정 넘김 / 5시간 / 10초 → 부분 실패 (Q-6) + 실기기 필요**
- 자정: Q-6.
- 5시간: 실기기 필요(위 표).
- 10초: 이동이 10m 미만이면 5번 가드, 그 이상이면 0.02km 짜리 기록이 정상 저장된다. 통과.

### [신발]

**13. 신발 등록 없이 러닝 → 통과 예상 (설계상 차단)**
신발 0켤레면 홈에 시작 CTA 자체가 없고(`EmptyHome`), 위젯 딥링크도 `App.tsx:2232` 에서
`effectiveId` 가 없으면 보류된다. 워치 단독 런이 들어와도 귀속할 신발이 없으면 기록을 만들지
않는다(`App.tsx:1759-1761` — 러닝 자체는 워치가 HealthKit 에 이미 남겼으므로 유실 아님).
shoe-first 제품 결정과 코드가 일치한다.

**14. 검색 0건 → 직접 추가 → 통과 예상**
`ShoePicker.tsx:245-256` 이 "직접 추가"를, `:260-278` 이 "내 신발이 없어요"(카탈로그 개선 요청)를
**서로 다른 행동으로** 분리해 둔다. 검색 미스 로깅은 900ms 디바운스 + 마지막 질의 기억으로
타이핑 중 잡음을 막고(`:117-128`), 실패는 삼켜 등록을 절대 막지 않는다.
등록 자체는 오프라인에서도 된다(신호 전송은 fire-and-forget, `:88`).

**15. 신발 삭제 후 그 신발의 러닝 기록 → 부분 실패 (Q-10)**
런은 살아남는다(`App.tsx:1050-1062` — cascade 삭제 없음). 통계·주간 거리에도 그대로 잡힌다.
문제는 표기 일관성과 90일 뒤 이름 소실이다.

**16. 같은 신발 중복 등록 → 실패 예상 (Q-8)**

**17. 수명 초과 신발로 계속 러닝 → 통과 예상**
`shoeHealth`(`lib/shoe.ts:131-151`)는 100% 를 넘겨도 참값을 그대로 돌려주고(`remainingKm` 만
0 으로 클램프), 링은 `primitives.tsx:477` 에서 `min(1)` 로 잘린다. 교체 알림은 신발별로 1회만
울리고(`lib/shoe.ts:213-229 reconcileShoeAlerts`), 리캡의 남은 수명도 `App.tsx:2462` 에서
음수가 되지 않는다. 러닝을 막지 않는다 — 의도된 동작이다.

### [입력·표시]

**18. 아주 긴 신발 이름 / 이모지 / 특수문자 → 실패 예상 (Q-9)**
크래시는 없다(`parseShoeName` 은 `lib/shoe.ts:22-32` 순수·throw 없음, 이모지는 첫 토큰이 브랜드가
된다). 깨지는 건 레이아웃이다.

**19. 시스템 글꼴 최대 크기 → 실기기 필요**

**20. 러닝 기록 1000개일 때 통계·목록 → 실패 예상 (Q-4·Q-12)**
목록 자체는 안전하다 — `HistoryScreen.rn.tsx:1139` FlatList 가상화 + 기본 5건만 노출(`:1059-1061`).
무너지는 건 목록이 아니라 **런 목록이 바뀔 때마다 도는 I/O 와 파생 계산**이다.

---

## 4. 새로 발견한 결함 (상세)

### Q-1 `BLOCKER` — 권한 거부로 시작조차 못 한 러닝을 종료하면 직전 러닝이 복제 저장된다

**경로.** `screens/RunEngine.tsx:396-411` 의 마운트 시퀀스는 권한이 거부되면 이렇게 끝난다:

```
RunEngine.tsx:404-408
if(!perm.foreground){
  openLocationSettingsAlert(...);
  setPermLost(true);
  return;              // ← beginRun() 을 부르지 않는다
}
```

`beginRun()` 을 안 부르면 `runTracker.start()` 도 안 불린다. 그런데 **엔진 상태를 초기화하는
유일한 지점이 `start()` 다**(`lib/runTracker.ts:226-289` — `dist=0`, `t0=now`, `pts=[]` 를 여기서만
리셋한다). `stop()` 은 `active=false` 로만 바꾸고 누적값을 그대로 둔다(`:292-300`).

`runTracker` 는 **프로세스 싱글턴**이다(`lib/runTracker.ts:884`). 그래서 직전 러닝의
`dist`·`t0`·`pts` 가 그대로 남아 있다.

**그 상태에서 종료 버튼은 살아 있다.** `permLost` 는 배너만 띄우고 `onStop` 을 막지 않는다
(`RunActiveScreen.rn.tsx:552`, `:147` — `onStop` 에 조건이 없다). 종료를 누르면:

```
RunEngine.tsx:730-731
const ft=runTracker.getElapsedFinal();          // ← 직전 러닝의 t0 기준 경과시간
const fk=trackMode?…:runTracker.getDistanceKm();// ← 직전 러닝의 누적 거리
if(fk<0.01){ …가드… }                            // ← 5.2km 라 통과해 버린다
…
RunEngine.tsx:748  const sampled=simplifyRoute(runTracker.getPoints()); // 직전 러닝의 경로
RunEngine.tsx:806  await onSave(Math.round(fk*100)/100, ft, …);
```

**결과.** 방금 고른 신발에 직전 러닝과 **같은 거리·시간·GPS 경로**를 가진 기록이 하나 더 생긴다.
신발 수명이 두 번 차감되고, 통계·PB·훈련부하가 전부 오염된다. 화면에는 0.00km 가 떠 있었으므로
사용자는 무엇이 저장됐는지도 모른다.

**재현 조건.** ① 이번 앱 실행에서 러닝을 한 번 완주했다 ② 그 뒤 설정에서 위치 권한을 껐다(또는
"앱을 사용하는 동안"에서 "허용 안 함"으로 바꿨다) ③ 다시 러닝을 시작하고 종료를 길게 눌렀다.
앱을 완전히 껐다 켜면 싱글턴이 초기화되므로 재현되지 않는다 — **한 세션 안에서만** 터진다.

**참고.** 같은 이유로 `getElapsedFinal()` 도 직전 러닝 시각을 기준으로 계산돼, 저장되는 시간이
"직전 러닝 시간 + 8초" 근처가 된다(`runTracker.ts:636-640` 의 `ongoingStallMs` 가 공백 대부분을
빼기 때문). 즉 숫자가 명백히 이상하지도 않아 **눈으로 걸러지지 않는다.**

**왜 지금까지 안 잡혔나.** 기존 감사는 "권한 거부 시 트래킹을 시작하지 않는가"를 봤고 그건
정확히 지켜지고 있다. 문제는 시작이 아니라 **종료 경로가 시작 여부를 확인하지 않는 것**이다.

---

### Q-2 `MAJOR` — 오프라인에서 회원 탈퇴를 누르면 아무 일도 일어나지 않는다 (영구 대기)

`lib/firebaseCloudPort.ts:291-341 deleteAccount` 는 백업 문서를 먼저 지운다:

```
firebaseCloudPort.ts:307   await deleteDoc(backupRef(user.uid));
```

Firestore 쓰기 프라미스는 **서버 ack 이후에 resolve** 한다(오프라인 영속이 켜져 있으면 로컬에는
즉시 반영되지만 프라미스는 pending 으로 남는다). 즉 오프라인에서 이 `await` 는 **reject 가 아니라
영원한 pending** 이다 — `try/catch` 가 잡을 수 있는 종류의 실패가 아니다.

호출부에는 타임아웃도 진행 표시도 없다:

```
ProfileScreen.rn.tsx:400-412
onPress: async () => {
  try { await onDeleteAccount(); …성공 메시지… }
  catch (e) { …실패 메시지… }
}
```

**결과.** 사용자가 '탈퇴'를 누르면 다이얼로그만 닫히고 **성공도 실패도 아무 메시지가 없다.**
`setCloudMsg` 가 영영 안 불린다. 앱스토어 5.1.1(v) 인앱 탈퇴 요건을 만족한다고 제출한 기능이
지하철 안에서는 고장 난 것처럼 보인다.

같은 원리로 오프라인에서 런을 삭제하면 `App.tsx:1210` 의 `await port.deleteRunDetail(sid)` 에서
멈춰 `:1217` 의 "러닝 기록 삭제됨" 토스트가 안 뜨고, `:1212` 의 마커 정리도 건너뛴다.
(런 자체는 그 전에 로컬에서 제거되므로 데이터 유실은 아니다 — 피드백만 사라진다.)

---

### Q-3 `MAJOR` — 완주 저장이 타임아웃 없는 역지오코딩을 기다린다

`lib/geocode.ts:35-42 reverseGeoLabelKo` 는 OS 지오코더를 부르고 **타임아웃이 없다.** 주석은
스스로 이렇게 적어 뒀다:

```
lib/geocode.ts:33   OS 지오코더는 네트워크 상태에 따라 수 초 걸릴 수 있어 호출부는 비차단으로 쓴다.
```

첫 fix 경로는 실제로 비차단이다(`RunEngine.tsx:366` `void reverseGeoLabelKo(...)`).
**그런데 저장 경로는 차단한다:**

```
RunEngine.tsx:796-806
if(!loc&&routeFin){ … loc = await reverseGeoLabelKo(lat, lon); }
…
await onSave(…, loc, …);        // ← 라벨을 받은 다음에야 저장
```

`handleSave` 도 같다(`:828-838`).

**결과.** 이 폴백은 `locationRef.current` 가 비었을 때만 도는데, 그게 비는 대표적 상황이 바로
**네트워크가 없어 첫 fix 지오코딩이 실패한 경우**다. 즉 오프라인 러닝에서는 매번 이 차단 호출을
탄다. 지오코더가 늦거나(레이트 리밋) 응답하지 않으면 완주 기록이 저장되지 않은 채 화면이 멈춘다 —
`finishRun` 에는 `saving` 표시도 없어서(세리머니는 이미 끝났다) 사용자에게는 **앱이 먹통이 된 것**으로
보인다. 위치 라벨은 장식이고, 러닝 기록은 정본이다. 우선순위가 뒤집혀 있다.

---

### Q-4 `MAJOR` — 런 1건당 AsyncStorage 왕복 1회, 그것도 `runs` 가 바뀔 때마다

```
App.tsx:851   // runs가 바뀔 때마다 multiGet으로 한 번에 읽고, …
App.tsx:858   const vals=await Promise.all(ids.map(id=>AsyncStorage.getItem('surface_'+id)));
```

**주석과 코드가 다르다.** `multiGet` 이 아니라 런 수만큼의 개별 브리지 호출이다.
이 저장소 래퍼에는 배치 API 가 있고 다른 곳에서는 쓰고 있다(`lib/runPersistence.ts:201,212`,
`App.tsx:948,1212` 의 `setMany`/`getMany`/`removeMany`). 여기만 빠졌다.

의존성이 `[runs]` 이므로(`App.tsx:866`) **클라우드 동기 1회·런 추가·편집·삭제·워치 런 수신마다**
전량이 다시 돈다. 런 1000건이면 한 번에 1000 왕복이다.

같은 시나리오에 하나 더 겹친다 — 거리 PB 마이그레이션은 **순차 await** 다:

```
lib/distancePBStore.ts:54-59
for (const id of ids) {
  if (cache[id] !== undefined) continue;
  const track = await safe(deps.loadTrack(id));   // ← 한 건씩 직렬
```

캐시가 있으면 건너뛰지만, **재설치·기기 변경 직후 첫 부팅**(클라우드에서 1000건을 받은 순간)에는
캐시가 비어 있어 1000회가 직렬로 돈다(`App.tsx:261` 이 `paceTrack_<id>` 를 개별 getItem 으로 읽는다).

---

### Q-5 `MAJOR` — 미완료 러닝 복구 다이얼로그가 로그인/부팅 게이트 위에 뜬다

```
App.tsx:869-899
useEffect(()=>{ … const snap=await loadSnapshot();
  if(asked||!isResumable(snap)||!snap) return;
  showDialog('완료하지 않은 러닝이 있어요', …);
},[]);
```

의존성이 `[]` 라 **마운트 즉시** 돈다. `authUser` 도 `bootState` 도 보지 않는다.
`DialogHost` 는 `Main` 바깥의 루트에 있으므로(`App.tsx:250`) 렌더 사다리가 무엇을 반환하든 그
위에 그려진다 — 즉 `BootSkeleton`(`:2280`)이나 `LoginScreen`(`:2283`) 위에 이 다이얼로그가 뜬다.

**이미 같은 부류의 사고를 한 번 겪고 고친 자리가 바로 옆에 있다:**

```
App.tsx:2090-2095
// 로그인 게이트를 통과하고 데이터가 준비된 뒤에만 띄운다(2026-07-30 Android 실측 발견).
// 안 그러면 **로그인 화면 위로** 알림이 뜬다 …
if(!authUser?.uid||bootState!=='ready')return;
```

알림에는 이 가드를 넣었는데 복구 다이얼로그에는 안 넣었다. 게다가 이쪽이 더 나쁘다 — 알림은
정보만 주지만 이 다이얼로그는 **선택을 요구**하고, 그 선택이 설정하는 `overlay==='run'` 은
렌더 사다리에서 인증·부팅 게이트보다 **아래**에 있어(`:2382`) 즉시 반영되지 않는다.

---

### Q-6 `MINOR` — 자정을 넘긴 러닝이 '끝난 날'로 기록된다

```
App.tsx:2406   const newId=await addRun(activeRun.id,km,today(),…);
```

`today()`(`App.tsx:201`)는 **저장 시점**의 로컬 날짜다. 23:30 에 출발해 00:20 에 끝낸 러닝은
다음 날 기록이 된다.

파급:
- 스트릭(`lib/stats.ts maxDayStreak`)과 홈 7칸 점(`App.tsx:2610 weekBuckets`)이 하루씩 밀린다.
- 주간 목표 집계가 주 경계에서 어긋난다(일요일 밤 러닝이 다음 주로 넘어간다).
- 러닝 리마인더의 `ranToday`(`App.tsx:1269-1272`)가 **달린 날에 "오늘 달릴 시간이에요"** 를 띄운다.

Strava·NRC·가민은 전부 **시작 시각** 기준이다. 엔진은 `t0` 를 이미 들고 있으므로
(`lib/runTracker.ts:182`) 정보가 없어서 못 하는 게 아니라 안 쓰고 있는 것이다.
워치 런은 반대로 **시작 시각**을 쓴다(`App.tsx:1762-1763` `p.startMs`) — 같은 앱 안에서 두 규칙이
공존한다.

---

### Q-7 `MINOR` — 카메라/앨범 권한 거부 시 무반응인 진입점 2곳

`lib/photo.ts:96`(`captureCertPhoto`)와 `:109`(`pickPhotoFrom('camera')`)는 권한 거부를 `null` 로만
돌려준다. 호출부가 그걸 그대로 삼킨다:

| 진입점 | 코드 | 거부 시 동작 |
|---|---|---|
| 신발 등록 → "영수증·박스 사진으로 자동 입력" | `AddShoeScreen.rn.tsx:70-72` `if (!photo?.uri) return;` | 아무 일도 안 일어남 |
| 대회 기록 → 기록증 촬영 | `RaceMedalScreen.rn.tsx:99-100` `if (!p) return;` | 아무 일도 안 일어남 |
| 메달 카메라 → "앨범에서 선택" | `MedalCamera.tsx:66-67` | 아무 일도 안 일어남 |

같은 파일이 **취소와 거부는 다르게 다뤄야 한다**고 스스로 적어 뒀고(`lib/photo.ts:19-20`),
프로필 사진 경로는 실제로 그렇게 구현돼 있다(`App.tsx:1678-1683` 설정 열기 안내).
`PhotoPick` 타입을 쓰는 경로만 제대로 되어 있고, `null` 반환 래퍼를 쓰는 세 경로가 빠졌다.

메달 카메라의 **촬영** 경로는 예외적으로 잘 되어 있다(`MedalCamera.tsx:74-88` 권한 안내 화면 +
앨범 폴백) — 그래서 더 눈에 띈다.

---

### Q-8 `MINOR` — 같은 신발을 몇 번이든 등록할 수 있고, 등록 후엔 구분할 방법이 없다

`App.tsx:985-1006 addShoe` 에 중복 검사가 없다. 이름·브랜드·모델 어느 것도 대조하지 않는다.
온보딩 경로(`App.tsx:2271-2273`)도 같은 함수를 지난다.

등록 뒤에 구분이 안 되는 게 더 문제다 — 화면들이 **이름만** 보여준다:
- 홈 캐러셀 카드: `screens/KeegoHome.tsx:229-230`(브랜드 + 모델)
- 수동 기록 폼 신발 칩: `HistoryScreen.rn.tsx:239` `label={`${sh.brand} ${sh.model}`}`
- 러닝화 피커 '내 러닝화': `ShoePicker.tsx:187`

누적 거리는 다르지만 칩에는 안 뜬다. 사용자가 어느 쪽에 기록을 붙이는지 알 수 없고, 잘못 붙이면
두 신발의 수명이 동시에 틀어진다. 로테이션 추천(`App.tsx:2111-2119`)도 같은 브랜드·모델 두 켤레를
서로 다른 신발로 취급해 "번갈아 신으세요"를 사실상 같은 신발에 대해 말하게 된다.

최소한 등록 시 "이미 등록한 신발이에요 — 그래도 추가할까요?" 한 겹은 필요하다(두 켤레를 진짜로
가진 사람도 있으므로 **차단이 아니라 확인**이 맞다).

---

### Q-9 `MINOR` — 입력 길이 제한 0 + 홈 히어로 브랜드 줄에 `numberOfLines` 없음

피커의 '기타' 직접 입력 두 칸에 `maxLength` 가 없다:

```
ShoePicker.tsx:284-298
<Input value={customBrand} onChangeText={setCustomBrand} placeholder="브랜드명을 입력하세요" … />
<Input value={customModel} onChangeText={setCustomModel} placeholder="모델명을 입력하세요" … />
```

검색창 직접 추가도 마찬가지다(`:247` `pickManual(selBrand, query.trim())` — `query` 무제한).

모델명은 대부분의 표시부가 `numberOfLines` 로 막아 준다(`KeegoHome.tsx:230`, `ShoesScreen.rn.tsx:293,537`,
`ShoePicker.tsx:154`). **브랜드 줄만 안 막혀 있다:**

```
screens/KeegoHome.tsx:229
<Text style={styles.cardBrand}>{shoe.brand}{cat ? ` · ${cat}` : ''}</Text>   ← numberOfLines 없음
```

`parseShoeName`(`lib/shoe.ts:22-32`)은 카탈로그 브랜드에 안 걸리면 **첫 토큰 전체**를 브랜드로
쓰므로, 공백 없는 긴 문자열이나 이모지 열을 입력하면 그게 통째로 브랜드가 되어 히어로 카드에서
줄바꿈된다. 이모지·특수문자 자체는 안전하다(순수 문자열 처리, throw 없음).

부수적으로 `firestore.rules` 는 `shoe_requests` 의 brand/model 을 60자로 제한하므로
(baseline 2-2 표) 긴 이름은 카탈로그 개선 신호가 **조용히 거부**된다 — 등록은 되고 신호만 사라지는
비대칭이 생긴다(`ShoePicker.tsx:88` 이 실패를 삼킨다).

---

### Q-10 `MINOR` — 삭제한 신발의 이름을 목록은 버리고 상세는 살린다

같은 데이터를 두 화면이 다르게 말한다:

| 화면 | 코드 | 표시 |
|---|---|---|
| 기록 목록 카드 | `HistoryScreen.rn.tsx:906-907` | "삭제된 신발" + 모델 자리 **빈칸** |
| 런 상세 | `HistoryScreen.rn.tsx:555-560` | "삭제된 신발" + **실제 신발명** |

`run.shoeName` 은 두 곳 다 들고 있다(`lib/appViewModel.ts:113` — 묘비까지 포함해 만든 `nameById`).
목록만 안 쓴다.

그리고 그 이름은 **90일 뒤 사라진다.** 신발 묘비는 TTL 로 정리되고(`lib/cloudSync.ts:449`
`TOMBSTONE_TTL_MS = 90일`, `:484-501 compactTombstones`), 정리된 결과가 동기 병합을 거쳐
로컬 묘비 상태로 되돌아온다(`App.tsx:1324-1335`). 즉 신발을 지운 지 3개월이 지나면 그 신발로
달린 **모든 과거 기록의 상세에서도** 이름이 사라진다. 이름 보존을 위해 일부러 `name` 을
남기도록 설계했는데(`lib/cloudSync.ts:503-504 SHOE_TOMBSTONE_KEEP`) TTL 이 그걸 되돌린다.

---

### Q-11 `MINOR` — 수동 기록 폼의 신발 칩에 보관(retired)한 신발이 그대로 뜬다

`App.tsx:2618` 이 `shoes={uiShoes}` 로 **보관 신발까지 전량** 넘기고, 폼은 필터 없이 칩을 만든다:

```
HistoryScreen.rn.tsx:234-244
{shoes.map((sh, i) => { … <Chip label={`${sh.brand} ${sh.model}`} … /> })}
```

홈·러닝 시작 경로는 `homeShoePairs`(`lib/appViewModel.ts:80-82` `.filter(x => !isRetired(x.raw))`)로
보관 신발을 정확히 걸러 낸다. 수동 입력 경로만 빠졌다.

보관은 "선택 목록에서 숨기되 기록은 보존"이 정의다(`App.tsx:1064-1065` 주석). 그런데 여기서는
숨겨지지 않아, 이미 은퇴시킨 신발에 새 거리를 붙일 수 있다 — 명예의 전당에 박제된 은퇴 스냅샷과
실제 누적 거리가 어긋나는 경로가 열린다.
(편집 모드에서 원래 신발이 보관 상태인 경우를 위해 **완전 제거는 곤란**하다. 보관분을 뒤로 밀고
'보관됨' 표식을 붙이는 쪽이 맞다.)

---

### Q-12 `MINOR` — 매 렌더 O(신발 × 런) 파생 계산이 메모 없이 돈다

`Main` 의 렌더 본문에서 매번 다시 계산되는 것들(런 1000건 · 신발 10켤레 기준):

| 계산 | 코드 | 비용 |
|---|---|---|
| 신발별 예측 | `App.tsx:1965-1966` `for(const s of shoes) homeForecasts[s.id]=forecastForRaw(s)` | 신발마다 런 전량 filter + 예측 → 10 × 1000 |
| 신발별 합계 | `App.tsx:2152-2163` `shoes.forEach(… runs.filter(…) …)` | 10 × 1000 + 페이스 집계 |
| 워치 페이로드 직렬화 | `App.tsx:1719`, `:1726` `JSON.stringify(buildWatchRecentRuns(runs,shoes))` | 매 렌더 전량 순회 + 직렬화 |
| PB 시그니처 | `App.tsx:266` `runs.map(...).join(',')` | 매 렌더 1000 요소 map + join |
| 동기 시그니처 | `App.tsx:1601-1604` | 매 렌더 두 배열 reduce |

`useMemo` 가 걸린 것도 있지만(`homeProgression`·`progressionCtx`·`homeLoad`) 위 다섯은 없다.
런이 늘수록 탭 전환·설정 토글 같은 **러닝과 무관한 상호작용**이 느려진다.

기록 화면에도 비슷한 게 있다 — `HistoryScreen.rn.tsx:1022-1024` 가 연도 수만큼 런 전량을 다시
filter 한다(3년치면 3 × 1000, 매 렌더).

---

## 5. 확인했고 문제 없던 것 (회귀 방지 기록)

다음은 시나리오상 의심 지점이었으나 **코드가 이미 옳게 처리**하고 있어 지적하지 않는다.
나중에 누가 다시 의심하지 않도록 근거만 남긴다.

| 의심한 것 | 실제 | 근거 |
|---|---|---|
| 러닝 중 권한 회수 시 시간이 계속 흐르는가 | 거리와 함께 동결된다 | `lib/runTracker.ts:404-415` (플래그보다 **먼저** 동결) |
| 재허용 후 설정 다녀온 시간이 경과시간에 더해지는가 | pausedMs 로 흡수한다 | `lib/runTracker.ts:425-443` |
| 일시정지 중 이동한 거리가 재개 첫 fix 에서 합산되는가 | `lastGood=null` 로 차단 | `lib/runTracker.ts:377` |
| 포그라운드/백그라운드 두 경로가 같은 fix 를 이중 계상하는가 | 타임스탬프 de-dupe | `lib/runTracker.ts:454-455` |
| 실내 모드에서 死구간 경고가 뜨는가 | 안 뜬다(개념 자체를 끈다) | `lib/runTracker.ts:644`, `RunEngine.tsx:940` |
| 0.00km 좀비 스냅샷이 저장되는가 | 두 겹 가드 | `RunEngine.tsx:732`, `:819` |
| 삭제한 런이 부팅에서 부활하는가 | 동기 영속 묘비로 필터 | `App.tsx:954-963` |
| 삭제한 런의 시계열 사이드카가 누수되는가 | 7종 전부 정리 | `App.tsx:1193-1202` |
| 신발 삭제가 런을 동반 삭제하는가 | 안 한다 | `App.tsx:1050-1062` |
| 경로 단순화가 아직 균등 스트라이드인가 | RDP 로 교체됨(허용오차 5m) | `lib/geo.ts:80-140` — **baseline 의 `MINOR` 미해결 항목이 해소됐다** |
| 시스템 글꼴 배율 상한이 없는가 | 본문 1.5× · 히어로 1.2× | `lib/text.tsx:27,29` · 직접 `Text` import 0건 |
| 런 1000건에서 목록이 전부 마운트되는가 | FlatList + 기본 5건 | `HistoryScreen.rn.tsx:1139`, `:1059-1061` |

---

## 6. 처리 결과 (2026-08-04)

BLOCKER 1건 + MAJOR 4건 **전부 수정 완료.** 각 커밋은 결함 하나만 다루고, 회귀 가드를
함께 넣었다.

| 커밋 | 다룬 것 | 함께 드러나 고친 것 | 회귀 가드 |
|---|---|---|---|
| `a2a3ddc` | **Q-1** 엔진 소유권 — 종료 경로가 "이 화면이 `start()` 를 불렀는가"부터 확인한다. '이어 달리기' 실패는 버리지 않고 검토 화면으로 넘긴다 | ① 정지된 엔진은 스냅샷을 쓰지 않는다(안 그러면 저장이 끝난 러닝이 `togglePause` 한 번에 "완료하지 않은 러닝"으로 되살아난다) ② `permLost` 배너가 일시정지 한 번에 사라지던 것 | `App.runStaleEngine.test.tsx` |
| `4d2d109` | **Q-5** 복구 프롬프트를 인증·부팅 게이트 뒤로 | 중복 방지를 effect 지역변수 → ref(이제 effect 가 다시 돈다) | `App.runrecoverGate.test.tsx` |
| `5516be0` | **Q-4** 노면 태그 `getMany` 1회 · PB 미스 16개씩 묶음 병렬 | — | `App.bulkStorageReads.test.tsx` |
| `91e0776` | **Q-3** 역지오코딩 제한 시간을 **모듈이 스스로** 지킨다(5s → 라벨 없이 진행) | 공용 `lib/withTimeout` 신설 — 시간 초과를 일반 실패와 구별 가능한 타입으로 | `lib/withTimeout.test.ts` · `lib/geocode.test.ts` |
| `aaf792b` | **Q-2** 탈퇴 파기 15s 제한. **시간 초과면 계정을 지우지 않는다**(닿지 못한 채 계정만 지우면 클라우드 데이터가 영영 남는다) | 런 삭제의 `deleteRunDetail` 무한 대기 → 8s 뒤 재시도 큐 | `lib/deleteAccountOffline.test.ts` |

**추가 — 감사 표에는 없었지만 민우님 질문에서 나온 것**(`9425376`)

민우님: "권한 끄고 달리면 '기록된 러닝이 없다'고 뜨는 게 맞아? 미리 알려줘야 되나."
맞는 지적이었다. 러닝 화면 진입 즉시 안내는 하고 있었지만 **순서가 늦었다** — 카운트다운
3·2·1·GO 를 다 돌린 뒤에야 물었다. `LocationPrimeScreen` 은 "다음 화면에서 위치와 동작·
피트니스 권한을 물어봐요"라고 약속해 놓고 실제로는 동작 권한만 요청했다.

확인하다 더 나쁜 것이 나왔다: **실내(트레드밀) 러닝이 위치 권한 때문에 시작조차 못 했다.**
실내는 GPS 를 켜지 않고 거리는 걸음이 정본인데(`lib/runTracker.ts:777-786`), 러닝 화면의
권한 게이트가 `indoor` 를 보지 않았다(`screens/RunEngine.tsx` 구 :425).

| 바뀐 것 | 내용 |
|---|---|
| 확인 시점 | 카운트다운 **전에** 세 갈래로 본다(허용/미결정/거부). 미결정 → 설명 화면, 그 '계속'이 **실제로** OS 에 묻는다 |
| 거부 시 | 설정 안내 2지선다(민우님 선택 — 잘못된 데이터가 생길 여지 0) |
| 설정 복귀 | **허용하고 돌아오면 그 러닝이 바로 시작된다**(민우님 제안). 홈부터 다시 짚게 하지 않는다 |
| 실내 | 위치를 묻지도 요구하지도 않는다 |
| `locPrimed` | 역할을 좁혔다 — "물어봤는가"의 진실은 앱의 기억이 아니라 OS 상태다 |

회귀 가드 `__tests__/App.locationGate.test.tsx`(3건). `App.coldstart` 의 '첫 러너' 테스트도
고쳤다 — 권한이 이미 `granted` 로 모킹돼 있었는데 신규 설치에선 `undetermined` 가 맞다.

게이트: `tsc` 0 에러 · `lint` 0 에러(경고 237/상한 **241** — 여유 4) ·
`test` 280 스위트 3,107개 전량 통과(3회 연속).
(baseline 이 적어둔 상한 338 은 낡았다 — `package.json` 은 `--max-warnings 241` 이다.)

**MINOR 6건도 전부 처리했다(2026-08-04)**

| 커밋 | 다룬 것 | 판단 |
|---|---|---|
| `25336ca` | **Q-11** 보관 신발을 뒤로 + '보관됨' 표식, 기본 선택은 활성 신발 · **Q-8** 중복 등록 확인 한 겹 + 이름이 겹칠 때만 누적 거리로 구분 · **Q-10** 목록도 묘비 이름을 살린다 | 보관 신발을 **지우지는 않는다**(옛 러닝 편집에 필요). 중복도 **막지 않는다** — 같은 모델 두 켤레는 로테이션에서 정상이고 로테이션이 이 앱의 차별점이다. 차단이 아니라 확인 |
| `407a0bf` | **Q-7** 카메라·앨범 거부 시 무반응이던 3곳 | `capturePhotoWithPermission` 으로 거부/취소를 갈랐다. 취소는 조용히(잘못한 게 없다), 거부만 안내. 같은 안내를 하던 자리 넷을 `showPermissionSettingsDialog` 로 모아 문구 분기도 막았다 |
| `83e2fc5` | **Q-12** 렌더 경로 O(신발×런) 다섯 곳 메모 · **Q-9** 입력 상한(60/30) + 브랜드 줄 `numberOfLines` | 상한 60/30 은 서버 규칙(`shoe_requests` ≤60자)과 정렬 — 넘기면 카탈로그 개선 신호가 조용히 거부돼 '등록은 되는데 신호만 사라지는' 비대칭이 생긴다 |
| `d79293a` | **Q-6** 러닝 날짜 = 시작한 날 | 워치 런 경로가 **이미** 시작 시각을 쓰고 있었다 — 같은 앱 안에서 두 규칙이 공존했다. 엔진이 `t0` 를 들고 있었으니 정보가 없어서 못 한 게 아니라 안 쓰고 있었다. 대회 감지·훈련 부하·인사이트의 날짜도 함께 맞췄다 |

**남은 것 = §2 실기기 8건뿐이다.** 특히 6·7번(잠금·백그라운드 연속성)은 이 앱의 코어인데
코드로 대체할 수 없다.

**별건으로 남긴 것**
- 삭제 신발 이름의 **90일 TTL 소실**(Q-10 후반) — 묘비 보존 기간 정책이라 이 감사의 범위 밖.
- **로그인 강제**(시나리오 4) — 기존 감사 3-6 이 이미 `MAJOR` 로 기록했다.
- `lint` 여유가 3이다(238/241). baseline 의 338 은 낡은 값이다.

**수정 후 실기기에서 확인할 것**(새로 넣은 경로라 눈으로 봐야 한다)

- 위치 권한을 끈 채 러닝 시작 → 일시정지 → 종료: "기록된 러닝이 없어요"가 뜨고 홈으로 나오는가,
  기록 탭에 아무것도 안 생겼는가.
- 러닝 중 크래시 → 재실행: 로그인 화면이 아니라 **홈에 도달한 뒤** 복구 프롬프트가 뜨는가.
- 비행기모드에서 회원 탈퇴: 15초 뒤 "연결이 불안정해…"가 뜨고 **계정이 남아 있는가**.
  연결을 켜고 다시 누르면 끝까지 진행되는가.
- 비행기모드 러닝 완주: 저장이 5초 안에 끝나고 위치 라벨만 비는가(기록은 온전한가).

---

## 7. 이 감사가 답하지 못한 것

- **실기기 항목 8건**(§2). 특히 6·7번(잠금·백그라운드 연속성)은 이 앱의 코어이므로 실측 없이는
  "통과 예상"이라고 쓸 수 없었다.
- **Q-1 의 실제 노출 빈도.** 한 세션 안에서 완주 → 권한 회수 → 재시작이라는 조건이 필요해
  드물어 보이지만, iOS 14+ 의 "앱을 사용하는 동안" 권한이 시스템에 의해 재확인되는 경로가 있어
  사용자가 의도적으로 끄지 않아도 도달할 수 있다. 빈도 추정은 실기기 몫이다.
- **오프라인 프라미스 pending 동작**은 `@react-native-firebase/firestore` v24 의 문서화된 계약을
  근거로 판단했다. 에뮬레이터로 한 번 확인해 두면 Q-2·Q-3 의 수정 범위가 확정된다.

---

*작성: Claude Code (QA 리드 관점 코드 감사) · 코드 수정 0 · 모든 지적에 `파일:줄번호` 근거 첨부*
