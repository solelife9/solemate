# 스토어 개인정보 표기 초안 (2026-07-19)

> App Store "앱 개인정보" + Google Play "데이터 보안" 설문에 그대로 옮기는 답안.
> 근거: 코드 실사(권한 문자열 Info.plist, 수집 지점 lib/*.ts). **광고·추적·IAP SDK 0** →
> **다른 앱/웹 추적 안 함**(IDFA 미사용, AdSupport 미링크 → ATT 프롬프트 불필요).
> 백엔드=Firebase(Auth·Firestore·Crashlytics·**Analytics**).
>
> ⚠️ **정정(2026-08-02, 심사 감사 B-2): Firebase Analytics 를 쓴다.** 2026-07-19 초안이
> "Analytics SDK 0 · Firebase Analytics 미사용"이라고 적어둔 것은 **오기**다 — 제품 계측
> (`lib/productAnalytics.ts`)이 그 뒤(2026-07-26 출시 심사 B-12)에 추가됐고, `package.json` 에
> `@react-native-firebase/analytics` 가 설치돼 이벤트 10종이 실제로 전송된다(호출부:
> `OnboardingScreen.rn.tsx` · `App.tsx` · `screens/RunEngine.tsx` · `ProfileScreen.rn.tsx`).
> `ios/SoleMate/PrivacyInfo.xcprivacy` 는 이미 `ProductInteraction`·`DeviceID` 를 선언하고
> 있었으므로(회귀 가드 `__tests__/nativePermissions.test.ts`), **이 문서만 낡아 있었다.**
> 아래 표에 두 항목을 추가했다 — 그대로 제출하면 바이너리 매니페스트와 콘솔 답변이 어긋난다.
>
> 계측은 개인 식별성을 낮추도록 설계돼 있다(자유 텍스트 금지 · 거리/시간은 버킷만 ·
> `setUserId` 미사용). 그래도 **"조심스럽게 수집"과 "수집 안 함"은 다르다** — 설문에는 수집으로 답한다.
>
> **원격 푸시(FCM) 미사용 — 푸시 토큰을 수집하지 않는다**(2026-07-30 결정,
> `lib/featureFlags.REMOTE_PUSH_ENABLED=false`). 알림은 전부 기기 안에서 처리된다:
> 러닝 리마인더는 OS 로컬 스케줄, 교체·주간 목표는 앱을 열 때 계산.
> → 설문에서 **'기기 또는 기타 ID'에 푸시 토큰을 넣지 말 것.** 알림 권한은 요청하지만
> 그건 기기 내 알림 표시용이다.

---

## A. App Store — 앱 개인정보(App Privacy)

### 1. 추적(Tracking)
- **이 앱은 사용자를 추적하지 않습니다.** (광고 식별자·데이터 브로커·크로스앱 추적 SDK 없음 → ATT 프롬프트 불필요)

### 2. 수집하는 데이터 (**판매·광고 목적 0**. 마지막 두 행만 '분석' 목적·연결 안 됨, 나머지는 전부 "사용자에게 연결됨 / App Functionality")

| 데이터 유형 | 세부 | 용도 | 연결됨? |
|---|---|---|---|
| **위치 - 정확한 위치** | 러닝 GPS 경로·거리·페이스 | 앱 기능(러닝 트래킹) | 예 |
| **건강 및 피트니스** | 심박(HealthKit 읽기), 워크아웃(쓰기), 케이던스·고도 | 앱 기능(러닝 분석·저장) | 예 |
| **연락처 정보 - 이메일** | 소셜 로그인(구글·애플·카카오·네이버)에서 받은 이메일 | 앱 기능(계정 식별·로그인) | 예 |
| **연락처 정보 - 이름** | 소셜 로그인의 표시이름(displayName) — 앱 안에서 '러너 이름'으로 표시 | 앱 기능(계정 표시) | 예 |
| **식별자 - 사용자 ID** | Firebase Auth UID | 앱 기능(클라우드 동기화·계정) | 예 |
| **진단 - 충돌 데이터** | Crashlytics 크래시 로그 | 앱 기능(안정성 개선) | 예* |
| **사용자 콘텐츠 - 기타 사용자 콘텐츠** | 이용자가 입력한 텍스트 — 신발 이름·러닝 메모·닉네임(백업), 신발 등록 요청 브랜드·모델명(`shoe_requests`) | 앱 기능(기록 보관·러닝화 목록 보강) | 예** |
| **사용자 콘텐츠 - 사진 또는 동영상** | **메달·기록증 사진만**(Firebase Storage `users/{uid}/medals/`). 대회 당일에만 남길 수 있어 재설치·기기교체로 잃으면 다시 만들 수 없다. 러닝화·러닝·프로필 사진은 **업로드하지 않는다** | 앱 기능(본인 기록 복원) | 예 |
| **기타 데이터 유형** | **나이·성별**(칼로리·심박존 계산용) | 앱 기능(운동 지표 정확도) | 예*** |
| **검색 기록(Search History)** | 러닝화 등록 화면에서 **결과가 0건인 검색어**(`search_misses`) | 앱 기능(러닝화 목록의 누락 모델 파악·보강) | **아니오**** |
| **사용 데이터 - 제품 상호작용** | 제품 계측 이벤트 10종 — 온보딩 단계, 첫 신발 등록, 러닝 시작/저장/폐기, 권한 수락 여부, 로그인 제공자, 동기 실패, 크래시 복구, 신발 은퇴 (`lib/productAnalytics.ts`) | **분석** + 앱 기능(개선) | **아니오**** |
| **식별자 - 기기 ID** | Firebase Analytics 앱 인스턴스 ID + App Check(DeviceCheck) 증명 토큰 | 분석 · 앱 기능(남용 방지) | 예***** |

\* Crashlytics 는 UID 와 연결될 수 있어 보수적으로 "연결됨"으로 신고. 광고/분석 목적 아님.

\*\* 신발 이름·메모·닉네임은 `userBackups/{uid}` 에 저장되므로 **연결됨**이 맞다. 같은 유형에
속하는 `shoe_requests`(신발 등록 요청)는 2026-08-03 부터 계정을 붙이지 않지만, 유형 단위
신고이므로 **보수적으로 연결됨**으로 답한다. 요청 기록은 다른 이용자에게 공개되지 않는다
(Firestore 규칙에서 읽기 전면 차단, 생성만 허용).

\*\*\* **체중·안정시심박은 위 '건강 및 피트니스'에 포함**된다. 나이·성별은 Apple 의 어느 기존
유형에도 들어가지 않아 **기타 데이터 유형**으로 신고한다(`App.tsx` 동기 페이로드의
`settings.age`/`settings.sex`). 셋 다 계산에만 쓰고 공개 프로필에는 **구조적으로 실리지 않는다**
(`lib/publicProfile.ts` 화이트리스트).

\*\*\*\* 두 항목은 **"연결 안 됨"** 으로 신고한다.
· **검색 기록** — 2026-08-03(`6852e02`)부터 계정 식별자를 함께 적지 않는다. Firestore 규칙이
  `keys().hasOnly(['query','createdAt'])` 로 강제하므로 코드가 되돌아가도 서버가 거부한다.
  **그 전에는 `userId` 를 붙였고 그때는 '연결됨'이 맞았다** — 신고를 바꾼 이유가 이것이다.
· **제품 상호작용** — `lib/productAnalytics.ts` 가 `setUserId` 를 쓰지 않아 계정과 행동 로그를
  잇지 않는다. 파라미터는 열거값과 버킷뿐이라 자유 텍스트(모델명·메모·닉네임)나 정확한
  거리·페이스는 보내지 않는다.
**추적(Tracking)은 여전히 "아니오"** — IDFA 를 쓰지 않고 AdSupport 도 링크돼 있지 않다.

\*\*\*\*\* 기기 ID 는 **보수적으로 "연결됨"**. Analytics 인스턴스 ID 자체는 계정과 잇지 않지만,
App Check 증명 토큰은 인증된 요청에 함께 실려 계정과 결부될 여지가 있다. 식별자는 과소 신고보다
과대 신고가 안전하다 — 아래 §3 의 '사진'(수집조차 하지 않는 것)과는 반대 방향의 판단이다.

### 2-0. 동의 시 다른 이용자에게 공개되는 정보 (2026-08-03 개봉)

이용자가 **명시적으로 동의한 경우에만** 아래가 서비스 내 다른 로그인 이용자에게 보인다
(`SocialConsentScreen` → `profiles/{uid}` · `leaderboards/{ym}/entries/{uid}`).
동의 전에는 아무것도 올라가지 않고, 끄면 올라가 있던 문서가 **내려간다**.

닉네임 · 누적/이번달 거리 · 러닝 횟수 · 거리 PB(5K·10K·하프·풀) · VO₂max 추정치 ·
평균 페이스 · 최장 거리 · 사용 중인 러닝화(브랜드·모델·누적거리·수명) · 은퇴 러닝화 · 월간 순위.

**경로·신체정보(체중·나이·성별·심박)·메모·사진·개별 러닝 일시는 공개되지 않는다** —
`lib/publicProfile.ts` 의 `PublicProfile` 타입 화이트리스트가 구조적으로 막는다.

**설문 답변에 미치는 영향:**
- **App Store** — 새 데이터 유형이 생기는 게 아니다(이름·기타 사용자 콘텐츠·피트니스는 이미
  신고). 애플 설문에는 '다른 이용자에게 공개' 칸이 없다. 대신 **처리방침에 고지가 있어야
  하고**, 그건 `privacy.html` 제4조의2로 반영했다.
- **Google Play — '데이터 공유'는 계속 "없음"이다.** Play 의 '공유'는 *다른 회사·조직으로
  전송*을 뜻한다. 같은 서비스의 다른 이용자에게 보이는 것은 여기에 해당하지 않는다.
  ⚠️ 이 판단을 나중에 뒤집지 말 것 — 뒤집으면 "제3자에게 판매·제공한다"는 잘못된 신고가 된다.

### 2-1. 정본 목록 (기계 대조용 — 손으로 고치지 말 것)

이 블록은 **테스트가 읽는다**(`__tests__/privacyLabelsSync.test.ts`). 위 표에 행을 더하거나
빼면 여기도 같이 고쳐야 하고, 그러면 `ios/SoleMate/PrivacyInfo.xcprivacy` 와 **집합이 정확히
일치하는지** 테스트가 대조한다.

**왜 이렇게까지 하나:** 이 문서는 두 번 낡았다. 2026-07-19 초안이 검색 기록을 "수집 안 함"으로
적어 뒀다가 07-30 에 정정했고, 같은 사유로 Firebase Analytics 가 08-02 까지 "미사용"으로
남아 있었다. **둘 다 "코드가 나중에 바뀌었는데 문서를 안 고쳐서"** 생겼다. 사람이 기억하는
방식으로는 세 번째가 온다 — 그래서 기계가 대조하게 만든다.

```privacy-labels
NSPrivacyCollectedDataTypePreciseLocation
NSPrivacyCollectedDataTypeHealth
NSPrivacyCollectedDataTypeFitness
NSPrivacyCollectedDataTypeUserID
NSPrivacyCollectedDataTypeEmailAddress
NSPrivacyCollectedDataTypeName
NSPrivacyCollectedDataTypeDeviceID
NSPrivacyCollectedDataTypeCrashData
NSPrivacyCollectedDataTypeProductInteraction
NSPrivacyCollectedDataTypeOtherUserContent
NSPrivacyCollectedDataTypeSearchHistory
NSPrivacyCollectedDataTypeOtherDataTypes
NSPrivacyCollectedDataTypePhotosorVideos
```

### 3. 수집하지 않는 것 (설문에서 "아니오")
- 결제 정보(인앱결제 없음), 정확한 광고 식별자, 브라우징 기록, 연락처 목록.
- ~~제품 사용 계측(Analytics)~~ → **정정(2026-08-02)**: 초안의 "Analytics SDK 0 · Firebase
  Analytics 미사용"은 오기다. 위 표에 **제품 상호작용 · 기기 ID** 로 신고한다(상단 정정 주석 참조).
- ~~검색 기록~~ → **정정(2026-07-30)**: 러닝화 검색 결과가 0건일 때 그 검색어를 남긴다
  (`search_misses`). 위 표에 **검색 기록**으로 신고한다. 2026-07-19 초안이 "수집하지 않음"으로
  적어둔 것은 그 기능이 나중에 추가됐기 때문이며, 그대로 제출하면 **실제 동작과 어긋난 신고**가 된다.
  → **재정정(2026-08-03)**: 계정 식별자를 빼면서 **'연결 안 됨'** 으로 바꿨다(각주 \*\*\*\*).
- ~~**사진 — 수집하지 않는다(정정 2026-08-02).**~~ → **재정정(2026-08-07): 사진을 신고한다.**
  **기능이 바뀌었다.** 메달·기록증 사진은 대회 당일에만 남길 수 있어 재설치·기기교체로
  잃으면 다시 만들 수 없다. 그래서 그 두 종류만 Firebase Storage(`users/{uid}/medals/`)에
  백업한다(`lib/photoCloud` · `lib/medalPhotoSync`).
  - **사용자 콘텐츠 › 사진 또는 동영상: 예 / 신원에 연결됨 / 앱 기능 / 추적 아님.**
  - 러닝화·러닝·프로필 사진은 **여전히 기기 밖으로 나가지 않는다** — 다시 찍을 수 있어서
    백업 대상이 아니다. 심사관이 "어떤 사진을 어디에 올리느냐"고 물으면 이 구분이 답이다.
  - `PrivacyInfo.xcprivacy` 에 `NSPrivacyCollectedDataTypePhotosorVideos` 를 추가했고
    회귀 테스트가 **이제는 그 존재를** 지킨다(`__tests__/nativePermissions.test.ts`).
  - 2026-08-02 의 "수집하지 않는다"는 **그때는 옳았다.** 낡은 판정이지 오판이 아니다 —
    이 저장소가 반복해 겪은 실패는 기능이 바뀌었는데 신고서가 그대로 남는 것이다.
- ~~이름 신고 여부는 선택~~ → **확정(2026-08-02): 신고한다.** `PrivacyInfo.xcprivacy` 가 이미
  `Name` 을 선언하고 있어, 선택으로 두면 바이너리와 콘솔 답변이 어긋난 채 제출된다.

---

## B. Google Play — 데이터 보안(Data Safety)

### 수집 및 공유
- **데이터 공유(3자 제공): 없음.** (Firebase 는 처리자/백엔드이지 "공유" 대상 아님)
- **데이터 수집: 예** (아래). 전송 중 암호화(HTTPS/TLS): 예. 사용자 삭제 요청 경로: 예(앱 내 계정 삭제).

| 카테고리 | 항목 | 수집 | 목적 |
|---|---|---|---|
| 위치 | 정확한 위치 | 예 | 앱 기능 |
| 건강 및 피트니스 | 건강 정보(심박·운동) | 예 | 앱 기능 |
| 개인 정보 | 이메일, 이름(표시이름) | 예 | 계정 관리·앱 기능 |
| 개인 정보 | **기타 정보**(나이·성별 — 칼로리·심박존 계산) | 예 | 앱 기능 |
| 사진 및 동영상 | **사진**(메달·기록증만) | **예** | 앱 기능(기록 복원) — 러닝화·러닝·프로필 사진은 업로드하지 않는다(§A 3) |
| 앱 활동 | (러닝 기록 등 앱 내 데이터) | 예 | 앱 기능 |
| 앱 활동 | **앱 내 검색 기록**(결과 0건인 러닝화 검색어 — 계정 미첨부) | 예 | 앱 기능(러닝화 목록 보강) |
| 앱 활동 | **기타 사용자 생성 콘텐츠**(신발 이름·러닝 메모·닉네임, 신발 등록 요청) | 예 | 앱 기능(기록 보관·목록 보강) |
| 앱 활동 | **앱 상호작용**(제품 계측 이벤트 10종 — 온보딩·러닝·권한·로그인 퍼널) | 예 | **분석**(서비스 개선) |
| 앱 정보 및 성능 | 크래시 로그·진단 | 예 | 앱 기능(안정성) |
| 기기 또는 기타 ID | 사용자 ID(UID) · **Analytics 앱 인스턴스 ID** | 예 | 앱 기능 · 분석 |

- **⚠️ 백그라운드 위치**: 화면 꺼도 러닝 추적 → Play 정책상 **백그라운드 위치 권한 선언 양식 + 용도 시연 영상** 요구될 수 있음(콘솔에서 안내). 용도 = "러닝 중 화면을 꺼도 거리·경로를 계속 기록".
- **계정 삭제**: 앱 내(마이 → 계정) 삭제 경로 있음 + 웹 삭제 안내 URL 필요 시 keego-legal 에 추가.

---

## C. Info.plist 권한 사용 설명 (이미 앱에 반영됨 — 검수용 사본)
- 위치(항상/사용중): "화면을 꺼도 러닝 거리·경로를 계속 기록하려면 위치 권한이 필요해요."
- 건강 읽기: "애플워치의 심박 데이터로 러닝 부하와 체력 분석을 정확하게 만들어요."
- 건강 쓰기: "Keego에서 기록한 러닝을 Apple 건강에 워크아웃으로 저장해요."
- 동작·피트니스: "케이던스(분당 걸음 수)와 고도 변화를 정확히 측정하기 위해 동작·피트니스 데이터를 사용해요."
- 사진 추가/접근·카메라: 공유 카드 저장·러닝화/메달 사진 등록·촬영.
- **마이크는 요청하지 않는다** — `NSMicrophoneUsageDescription` 을 의도적으로 두지 않았고
  (앱은 오디오를 녹음하지 않는다) 회귀 가드가 있다(`__tests__/nativePermissions.test.ts`).
  2026-07-19 초안에 "마이크"가 적혀 있던 것은 오기다(정정 2026-07-30).

## D. 공개 문서 (라이브 확인 2026-07-30 — 3개 전부 200)
- 개인정보 처리방침: https://solelife9.github.io/keego-legal/privacy.html
- 이용약관: https://solelife9.github.io/keego-legal/terms.html
- 계정 삭제 안내: https://solelife9.github.io/keego-legal/delete-account.html
- App Store Connect·Play Console 개인정보 URL 칸에 위 privacy.html 링크 입력.
- ⚠️ **`docs/privacy.html` 을 고쳤으면 공개 저장소 `solelife9/keego-legal` 에도 푸시해야 한다**
  (별도 저장소 호스팅 — 이 저장소만 고치면 공개 URL 은 옛 내용 그대로다).
  2026-07-30 수집항목 추가분(검색어·등록 요청)이 아직 반영되지 않았다면 지금 푸시할 것.
