# DESIGN.md — Keego 디자인 정본

> **앱의 모든 화면·컴포넌트·비주얼 결정의 단일 기준.** 코드 정본은 `theme.ts`(토큰) +
> `primitives.tsx`(컴포넌트)이고, 이 문서는 그 값들이 "왜 그런지"와 조합 규칙을 못박는다.
> 충돌 시 우선순위: 사용자 확정 결정 > 이 문서 > 과거 문서(`.tenet/DESIGN.md`는 기록용).
> 상위 철학은 `MISSION.md` §4 — "Never make the interface more colorful. Make it more premium."

## 1. 정체성 (2026-07-09 확정)

**다크 무채 글래스 + 절제된 파파야 포인트.** 프리미엄은 재질·타이포·절제에서 나온다(Apple/WHOOP).

- 배경 `BG #0A0A0A` · 카드 `CARD #1C1C1E` / `CARD_HI #2C2C2E` · 강조 `ACCENT #FFFFFF`(흰=강조)
- **브랜드색 `BRAND = RING_ACCENT #FF8000`(McLaren 파파야)**, 링 그라데이션 `#FFB458→#E56600`.
  **허용 범위(2026-07-09 'B 서명+진행' 확정)**: 러닝 진행 링 + ① 브랜드 서명(keego 워드마크)
  + ② 러닝 에너지·진행 지표(주간 목표 게이지 등). 가드레일 — 넓은 면(버튼 채움·카드 배경)
  금지, 상태색 대체 금지, 한 시야에 파파야 포인트는 소수만. 바이올렛 전역 액센트는
  "남발돼 튄다" 실기기 피드백으로 폐지(같은 실수를 파파야로 반복하지 않는다).
- **색은 의미에만**: 컨디션 GOOD/WARN/DANGER/BEST, 토글 ON=GOOD, 티어색, 심박존.
  장식 목적의 유채색 금지. HoF/Legend 골드는 성취 도메인 전용.
- 워드마크: Helvetica Neue Medium 소문자 `keego` **BRAND 파파야**(`KeegoWordmark` 단일 소스).

## 2. 글라스 재질 (2026-07-09 사용자 확정 — 목업 'B 대각 밸런스')

카드·CTA·칩이 공유하는 유일한 유리 문법. 값의 단일 진실원 = `theme.GLASS`,
구현 = `primitives.GlassEdge`. **화면에서 유리를 인라인으로 재구현하는 것 금지.**

- **표면**: 반투명 흰 채움 — 기본 `GLASS.fill`(5%) · 활성/히어로 `GLASS.fillActive`(7%) ·
  CTA `GLASS.fillCta`(10%). 불투명 판(#242426류) 위 유리 효과 금지.
- **림(빛)**: 코너 글린트 2점 — **좌상 30%(주광) · 우하 24%(반사)**, 반경 = 긴 변의 0.8로
  인접 두 변을 따라 길게 자연 감쇠(4스톱 이징), **우상·좌하 코너에선 거의 소멸**. 전 둘레
  헤어라인은 2%(거의 안 보이는 최소 존재감). 각 글린트는 코너 중심 방사형이라 종횡비와
  무관하게 성립한다. (구 대각선 위치투영은 넓은 버튼에서 좌우 변이 꺼지는 붕괴로 폐지;
  우상/좌하 글린트·6% 헤어라인은 기기 검증에서 "빛에서 멀어져도 라인이 남는다"로 폐지.)
- **광택(sheen)**: 표면 상단 3.5% → 42% 지점 소멸.
- **활성 표시 = 재질로**: 굵은 활성 보더(흰 55%류) 금지 → 채움 +0.02 + 림 `intensity 1.5`.
- **효과는 겹치지 않는다**: 표면 그라데이션·컨디션색 글로우·과한 그림자와 유리를 포개지 않는다.
  그림자는 "살짝 뜨는 정도"(예: 홈 히어로 opacity 0.3 · radius 14)까지만.
- 진짜 블러(BlurView)는 하단 탭바 독 등 오버레이 전용(Android 폴백 불투명).
- **배경 앰비언트(확정 '나')**: 4개 탭 루트에 `AmbientBackdrop`(상단 무채 광 7%→0, 화면
  45% 높이) — 유리 너머로 비칠 명암. 색 틴트 금지(무채만).
- **카드 2위계(확정 · 2026-07-10 헤어라인 통일)**: 일반 콘텐츠 카드 = `Card`(quiet —
  표면 + **코너 페이드 헤어라인**: `GlassEdge glints={false}`, 좌상·우하에서 발원한
  베이스 방사가 우상·좌하 코너에서 0으로 소멸 — 균일 RN 보더(`borderWidth/borderColor`)
  폐지), 화면당 소수의 주인공만 hero(표면 +글린트 림). 전부 반짝이면 아무것도 반짝이지
  않는다. 예외(RN 보더 유지): 바텀시트/모달/드럼피커·입력 필드(TextInput 행)·작은
  칩/필/버튼(눌림·선택 상태)·세그먼트 셀·의미색 보더(조건색/액센트/레어리티/HoF 골드).

## 3. 타이포그래피

- **Pretendard 단일 패밀리**(`FONT`=`DISPLAY`=PretendardVariable). 제2 디스플레이 폰트 금지.
- **스케일은 `theme.TYPE` + `theme.HERO`가 유일한 권위** (과거 문서의 56/28/22/15/12.5
  스케일은 폐기): `display 33 · title1 27 · title 23 · heading 18 · body 16 · label 14 ·
  caption 13 · micro 11` + 대형 숫자 `HERO 40/56/76`. 정수만(반px 금지), `rf()` 경유.
  2026-07-09 수렴 스윕 완료 — 39종 하드코딩 504건을 이 스케일로 수렴(`TYPE.*.fontSize`
  참조). 예외로 남긴 특수값: 차트 눈금 6~9, 러닝 초대형 숫자(37·42~52·68·96·104·150).
- 숫자는 항상 `fontVariant: ['tabular-nums']`. 단위(km·spm)는 숫자보다 작게, baseline 정렬.
- weight는 TYPE 프리셋 번들 사용. 화면에서 `fontSize: rf(임의숫자)` 신규 작성 금지 —
  스케일에 없는 크기가 필요하면 스케일을 고칠지부터 논의.

## 4. 간격 · 모서리 · 레이아웃

- 간격 = `SPACE`(4/8/12/16/20/32) 4배수 그리드. 좌우 거터 `GUTTER 20`. 카드 패딩 16,
  섹션 간 20~24. 신규 코드에서 홀수 임의값(7/13/15/17…) 금지.
- 모서리 = `RADIUS`(sm 12 / input 14 / md 16 / btn 18 / lg 20 / xl 24 / pill 999).
  리터럴 999 대신 `RADIUS.pill`.
- 단일 컬럼, SafeArea 준수, 하단 유리 탭바 독 + `TABBAR_CLEARANCE`(118) 스크롤 여백.
- **콘텐츠 리스트는 상단정렬 기본** — 세로 공백을 통째 센터링으로 숨기지 않는다
  (0개 빈 상태의 중앙 히어로만 관용). 단, 기존 6개 감사화면 중앙정렬은 사용자 '유지' 결정.
- 홈 = shoe-first 척추: 신발 히어로(수명 링) → 러닝 시작. 이 구조는 불가침.
- **신발 표현 = 추상 확정(2026-07-10 사용자 결정)**: 사진·모델별 실루엣 전면 폐기 —
  신발은 **글리프 + 수명 링 + 타이포(브랜드·모델명)**로만 말한다. 근거: 실사진은
  저작권·짝짝이·유지보수로 비현실, 이미지가 붙는 순간 프리미엄이 지저분해진다.
  브랜드 시그니처는 대신 파파야 링·코스맵·워드마크가 담당한다.

## 5. 컴포넌트 (primitives.tsx — 인라인 재구현 금지)

| 용도 | 프리미티브 | 규칙 |
|---|---|---|
| 카드 서피스 | `Card` | 배경+보더+radius 인라인 조립 금지(마이그레이션 진행 중) |
| CTA | `Button` | 유리 표면 + GlassEdge. 오렌지 솔리드 금지 |
| 유리 림 | `GlassEdge` | §2 값 고정. 활성은 `intensity` |
| 진행 링 | `Ring` | 러닝 링만 파파야, 그 외 무채/컨디션색 |
| 탭바 | `TabBar` | 떠 있는 유리 독, 활성=슬라이딩 오벌(색 아닌 형태) |
| 세그먼트/칩/필 | `SegmentedControl`/`Chip`/`Pill` | 선택면 = 글래스/CARD_HI |
| 수치 | `Stat`/`StatGrid`/`Metric` | tabular-nums·단위 분리 |

## 6. 모션 · 상태

- 마운트 진입 = Rise(아래→위 14px 페이드, 60~120ms 스태거). 게이지/링 = 차오르는 스윕
  (홈 링 1400ms — 급하면 멋이 죽는다는 사용자 피드백).
- 누름 = scale 0.97~0.98. 글로우/네온 금지.
- 진입 모션 = `Rise`(primitives) — 모든 탭 메인 스크롤에 적용(2026-07-10 완료).
- **로딩 = 고스트 스켈레톤(스피너 폐지)**: 빈 상태와 같은 실루엣 언어로 '곧 채워질 자리'를
  보여준다(예: 명예의 전당 랭킹 로딩 = 고스트 랭킹 행 페이드). ActivityIndicator 는
  버튼 내 인라인 진행(로그인 등)에만 허용.
- **빈 상태 = 에디토리얼 타이포 + 고스트 1~2장(2026-07-10 최종 확정)**:
  `EmptyGhostHeader`(타이틀 23·2줄 허용 — 타이포가 주인공) + 실카드 재질의 고스트
  실루엣 **1~2장만**(둘째는 45% 딤). 상단 시작. 고스트 스택 남발(3장+ 페이드)·중앙
  히어로·안내문 박스·점선 전부 기각을 거쳐 확정. 카피는 keep-going 보이스 + 다음 행동
  (예: 보관함 "당장 안 신는 신발은, 잠시 쉬게 하세요").
  **히어로형 화면(홈·신발탭)은 고스트 스택 없음** — GhostShoeCard 하나 + 여백이 비율.
  예외: 러닝화 아카이브(전당)의 황금 세리머니 빈 상태는 성취 도메인 set piece로 유지.

## 7. 톤 · 진실성

- UI 문구 전부 한국어, 톤 벤치마크 Apple Fitness · WHOOP · PS 트로피 · Spotify Wrapped.
  **유치/밈/RPG 금지.**
- **Truth only**: 모든 숫자는 실제 집계. 가짜 경쟁자·날조 업적 금지.
- 업적 3원칙: 안전 정렬(권고 처벌 금지) · 프리미엄 절제 · 직관적 점진.

## Iron Law (위반 시 머지 금지)

1. 화면 내 색·폰트·간격 하드코딩 0 — 토큰만. (기존 위반은 태스크 #3~#5로 상환 중)
2. 유리·카드·버튼은 primitives 경유 — 화면별 재발명 금지.
3. 시각 중요 변경은 목업(아티팩트) 승인 → RN 반영 → 기기 검증 순.
4. 러닝 화면은 목업 대신 기기 빌드로 반복(사용자 규칙).
